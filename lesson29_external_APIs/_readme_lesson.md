# Конспект лекции: Интеграция внешних API

## 1. Event-Driven Architecture (Повторение)

### Основные понятия
**Event-Driven Architecture (EDA)** — событийно-ориентированная архитектура, где компоненты обмениваются информацией через поток событий, а не через прямые вызовы (request-response).

### Компоненты EDA

**Событие (Event)**
- Запись о факте, который произошел в системе
- Неизменяемое (immutable)
- Содержит минимальную информацию: тип события, время, связанные данные (например, user ID)

**Производитель (Producer)**
- Компонент, который создает и отправляет события
- Не знает, кто будет потреблять эти события

**Потребитель (Consumer)**
- Компонент, который слушает определенные типы событий и реагирует на них

**Брокер сообщений / Шина событий**
- Промежуточный слой
- Принимает события от производителей
- Доставляет их потребителям

### Выбор брокера сообщений

**Факторы выбора:**
- Объемы данных
- Нужно ли сохранять сообщения
- Нужен ли доступ нескольких потребителей к одному сообщению одновременно

**Event Bus**
- Для базового уровня
- Компоненты подписываются на события через `.on()`
- Публикация через `.emit()`
- Часто используется с **Shared Queue** для коммуникации между потоками/процессами

---

## 2. Брокеры сообщений

### RabbitMQ

**Характеристики:**
- Классическая очередь (Queue-based решение)
- Ориентирован на надежную доставку сообщений между независимыми сервисами
- Синхронность и очередность

**Компоненты:**
- **Очередь (Queue)** — хранит сообщения, пока потребитель их не заберет
- **Обменник (Exchange)** — принимает сообщения от производителей и маршрутизирует их по очередям
- **Ключ маршрутизации (Routing Key)** — определяет, в какую очередь отправить сообщение

**Особенности:**
- **Message Retention** — сообщение удаляется сразу после подтверждения потребителем
- Сообщение доставляется только одному потребителю (хотя несколько могут конкурировать за одну очередь)
- Принцип **FIFO** (First In, First Out) в пределах одной очереди

**Применение:**
- Небольшие проекты
- Асинхронная доставка сообщений

---

### Apache Kafka

**Характеристики:**
- Распределенная платформа потоковой передачи событий
- Спроектирована для высокой пропускной способности и отказоустойчивости
- Идеальна для работы с огромными объемами данных в реальном времени

**Компоненты:**
- **Топик (Topic)** — категория или имя потока событий
- **Партиция (Partition)** — топик разделяется на партиции для параллельной обработки и масштабирования
- **Смещение (Offset)** — уникальный и последовательный ID события в пределах партиции
- **Consumer Groups** — несколько потребителей работают вместе для обработки одного топика

**Особенности:**
- **Message Retention** — сохраняет события часами, днями, годами (журнал событий)
- **Множественные потребители** — независимые группы могут читать один топик
- Потребители контролируют свое смещение (offset)
- **Ordering** — гарантируется в пределах одной партиции

**Применение:**
- Журналирование событий
- Потоковая обработка
- Аналитика
- Большие объемы данных
- Много потребителей в проекте
- Более масштабные и продвинутые решения

---

### Сравнение RabbitMQ vs Kafka

| Критерий | RabbitMQ | Kafka |
|----------|----------|-------|
| Сложность | Простой | Сложнее |
| Скорость | Относительно быстро | Очень быстро |
| Endpoints | Много отдельных очередей | Один топик с партициями |
| Message Retention | Удаляется после подтверждения | Хранится долго (часы/дни/годы) |
| Потребители | Один (конкурирующие) | Множественные группы |
| Применение | Веб/мобильные приложения | Масштабные системы, аналитика |

---

## 3. Что такое API?

### Определение
**API (Application Programming Interface)** — интерфейс программирования приложений, набор правил и инструментов, которые позволяют разным программам взаимодействовать между собой.

### Аналогии из жизни

**Официант в ресторане:**
- Клиент (вы) → делает заказ
- Официант (API) → записывает заказ и передает на кухню
- Кухня (сервер) → готовит блюдо
- Официант (API) → приносит готовое блюдо обратно

**Магазин:**
- Производитель → создает товар
- Магазин (API) → посредник
- Покупатель → получает товар

**Банковский терминал:**
- Терминал на кассе (клиент) → отправляет запрос
- API → передает запрос банковскому серверу
- Сервер → обрабатывает платеж
- API → возвращает результат терминалу

### Роль API

API позволяет:
- **Быть посредником** между клиентом и сервером
- **Получать запросы** от клиента
- **Передавать запросы** серверу
- **Формировать ответ** на сервере
- **Возвращать ответ** клиенту

---

## 4. Применение API

### Основные возможности

**Получение данных:**
- Погода на сегодня
- Курс валют
- Новости

**Отправка данных:**
- Создание поста в соцсетях
- Отправка команд (например, открытие ворот через Telegram bot)

**Интеграция сервисов:**
- Объединение разных систем
- Автоматизация процессов

**Работа с базой данных:**
- API как защитная прослойка между фронтендом и БД
- Не отправляем запросы напрямую к БД с фронта (небезопасно!)

### Примеры использования

- **Google Maps API** — отображение карт, построение маршрутов
- **Facebook API** — авторизация, публикация контента
- **Telegram Bot API** — создание ботов
- **Платежные системы** — Stripe, PayPal, LiqPay

---

## 5. Типы API

### REST API

**Характеристики:**
- Самый популярный тип
- Использует HTTP протокол
- Формат данных: JSON или XML
- Простая структура
- Поддерживает CRUD операции

**Преимущества:**
- Простота использования
- Легко масштабируется
- Относительно быстрый

**Применение:**
- Веб-приложения
- Мобильные приложения

---

### SOAP API

**Характеристики:**
- Более формальный протокол
- Использует только XML
- Встроенные стандарты безопеки
- Сложнее в реализации

**Преимущества:**
- Высокий уровень безопасности
- Надежность

**Применение:**
- Банковские системы
- Правительственные организации
- Корпоративные системы

---

### GraphQL

**Характеристики:**
- Язык запросов для API
- Разработан Facebook
- Формат: только JSON
- Один endpoint вместо множества

**Преимущества:**
- Клиент запрашивает только нужные данные
- Очень гибкий
- Очень быстрый (не загружает лишнее)

**Применение:**
- Современные веб-приложения
- Приложения с множеством типов запросов

---

### Сравнительная таблица

| Критерий | REST | SOAP | GraphQL |
|----------|------|------|---------|
| Формат данных | JSON/XML | XML | JSON |
| Сложность | Простой | Сложный | Средняя |
| Скорость | Быстро | Медленнее | Очень быстро |
| Endpoints | Много | Много | Один |
| Безопасность | Базовая | Высокая | Средняя |

---

## 6. REST API - Подробно

### CRUD операции

**CRUD** — четыре базовые операции с данными:
- **C**reate — создание
- **R**ead — чтение
- **U**pdate — обновление
- **D**elete — удаление

### HTTP методы

#### GET — получение данных
```
GET /api/users
GET /api/users/123
```
- Получает данные без изменения
- Может иметь параметры запроса
- Безопасный метод (не меняет состояние сервера)

#### POST — создание данных
```
POST /api/users
Body: {"name": "Иван", "email": "ivan@example.com"}
```
- Создает новый ресурс
- Данные в теле запроса
- Не идемпотентный (повторный вызов создаст новый ресурс)

#### PUT — полное обновление
```
PUT /api/users/123
Body: {"name": "Иван Петров", "email": "ivan@example.com"}
```
- Обновляет все поля ресурса
- Идемпотентный

#### PATCH — частичное обновление
```
PATCH /api/users/123
Body: {"email": "newemail@example.com"}
```
- Обновляет только указанные поля
- Более эффективен

#### DELETE — удаление
```
DELETE /api/users/123
```
- Удаляет ресурс
- Идемпотентный

---

### HTTP статус-коды

#### 2xx — Успех
- **200 OK** — запрос выполнен успешно
- **201 Created** — ресурс создан
- **204 No Content** — успешно, но без данных в ответе (часто при DELETE)

#### 4xx — Ошибки клиента
- **400 Bad Request** — некорректный запрос
- **401 Unauthorized** — требуется авторизация (нет токена)
- **403 Forbidden** — доступ запрещен
- **404 Not Found** — ресурс не найден
- **429 Too Many Requests** — превышен лимит запросов

#### 5xx — Ошибки сервера
- **500 Internal Server Error** — внутренняя ошибка сервера
- **502 Bad Gateway** — ошибка шлюза
- **503 Service Unavailable** — сервис недоступен

---

## 7. Работа с библиотекой requests

### Установка
```bash
pip install requests
```

### Основные операции

#### GET запрос
```python
import requests

# Простой GET
response = requests.get('https://api.example.com/users')
print(response.status_code)  # 200
print(response.json())

# GET с параметрами
params = {'city': 'Kyiv', 'limit': 10}
response = requests.get('https://api.example.com/data', params=params)
```

#### POST запрос
```python
data = {
    'name': 'Иван',
    'email': 'ivan@example.com'
}
response = requests.post('https://api.example.com/users', json=data)
```

#### PATCH запрос
```python
data = {'email': 'newemail@example.com'}
response = requests.patch('https://api.example.com/users/123', json=data)
```

#### DELETE запрос
```python
response = requests.delete('https://api.example.com/users/123')
if response.status_code == 204:
    print("Пользователь удален")
```

### Обработка ошибок
```python
try:
    response = requests.get('https://api.example.com/data', timeout=5)
    response.raise_for_status()  # Вызовет ошибку для 4xx/5xx
    data = response.json()
except requests.exceptions.Timeout:
    print("Время ожидания истекло")
except requests.exceptions.HTTPError as e:
    print(f"HTTP ошибка: {e}")
except requests.exceptions.RequestException as e:
    print(f"Ошибка запроса: {e}")
```

---

## 8. Аутентификация API

### API Key
Простейший способ — передача ключа в заголовках или параметрах:

```python
# В заголовках
headers = {'X-API-Key': 'your_api_key_here'}
response = requests.get('https://api.example.com/data', headers=headers)

# В параметрах
params = {'api_key': 'your_api_key_here'}
response = requests.get('https://api.example.com/data', params=params)
```

### Bearer Token (JWT)
```python
token = 'your_jwt_token_here'
headers = {'Authorization': f'Bearer {token}'}
response = requests.get('https://api.example.com/protected', headers=headers)
```

### Basic Authentication
```python
from requests.auth import HTTPBasicAuth

response = requests.get(
    'https://api.example.com/data',
    auth=HTTPBasicAuth('username', 'password')
)

# Короткий вариант
response = requests.get(
    'https://api.example.com/data', 
    auth=('username', 'password')
)
```

### OAuth 2.0
Используется в Google, Facebook, GitHub и других сервисах.

```python
# Получение токена
token_url = 'https://oauth.example.com/token'
data = {
    'grant_type': 'client_credentials',
    'client_id': 'your_client_id',
    'client_secret': 'your_client_secret'
}
token_response = requests.post(token_url, data=data)
access_token = token_response.json()['access_token']

# Использование токена
headers = {'Authorization': f'Bearer {access_token}'}
response = requests.get('https://api.example.com/data', headers=headers)
```

---

## 9. Практическая работа: Telegram Bot с Weather API

### Задача
Создать Telegram бота, который получает погоду через OpenWeatherMap API.

### Шаги реализации

#### 1. Получение токенов

**Telegram Bot Token:**
- Найти в Telegram: @BotFather
- Команда: `/newbot`
- Следовать инструкциям
- Сохранить полученный token

**OpenWeatherMap API Key:**
- Зарегистрироваться на https://openweathermap.org
- Раздел API keys
- Скопировать ключ
- ⚠️ Подтвердить email (иначе ключ не работает!)

#### 2. Настройка .env файла

```env
TELEGRAM_BOT_TOKEN=your_telegram_token_here
OPENWEATHER_API_KEY=your_openweather_key_here
```

**Важно:** В .env файле НЕ используйте кавычки и пробелы вокруг `=`

#### 3. Базовая реализация бота

```python
import os
import requests
from dotenv import load_dotenv
import telebot
from telebot import types

# Загрузка переменных окружения
load_dotenv()

# Инициализация бота
bot = telebot.TeleBot(os.getenv('TELEGRAM_BOT_TOKEN'))

# Список доступных городов
available_cities = ['Киев', 'Днепр', 'Львов']

# Базовый URL для API
BASE_URL = "http://api.openweathermap.org/data/2.5/weather"

def get_weather_data(city):
    """Получает данные о погоде"""
    params = {
        'q': city,
        'appid': os.getenv('OPENWEATHER_API_KEY'),
        'units': 'metric',
        'lang': 'ru'
    }
    
    try:
        response = requests.get(BASE_URL, params=params, timeout=10)
        
        if response.status_code == 200:
            return response.json()
        else:
            return None
            
    except requests.exceptions.RequestException as e:
        print(f"Ошибка: {e}")
        return None

def format_message(weather_data):
    """Форматирует сообщение о погоде"""
    if not weather_data:
        return "Не удалось получить данные о погоде"
    
    city = weather_data['name']
    temp = weather_data['main']['temp']
    humidity = weather_data['main']['humidity']
    description = weather_data['weather'][0]['description']
    
    message = f"""
🌍 Погода в городе {city}
🌡 Температура: {temp}°C
💧 Влажность: {humidity}%
📝 Описание: {description.capitalize()}
"""
    return message

@bot.message_handler(commands=['start'])
def send_welcome(message):
    """Обработчик команды /start"""
    # Создание клавиатуры с кнопками
    markup = types.ReplyKeyboardMarkup(resize_keyboard=True)
    
    for city in available_cities:
        markup.add(types.KeyboardButton(city))
    
    bot.send_message(
        message.chat.id,
        "Привет! Выбери город для получения погоды:",
        reply_markup=markup
    )

@bot.message_handler(func=lambda message: message.text in available_cities)
def handle_city_request(message):
    """Обработчик выбора города"""
    city = message.text
    
    # Получение данных о погоде
    weather_data = get_weather_data(city)
    
    # Форматирование и отправка сообщения
    response_message = format_message(weather_data)
    bot.send_message(message.chat.id, response_message)

# Запуск бота
if __name__ == "__main__":
    print("Бот запущен...")
    bot.polling(none_stop=True)
```

---

## 10. Важные моменты и рекомендации

### Безопасность

**Хранение токенов и ключей:**
- ✅ Всегда используйте `.env` файл
- ✅ Добавьте `.env` в `.gitignore`
- ❌ Никогда не коммитьте токены в репозиторий
- ❌ Не передавайте токены в URL параметрах (только для открытых API)

**Формат .env файла:**
```env
KEY=value
```
- БЕЗ кавычек
- БЕЗ пробелов вокруг `=`

### Обработка ошибок

**Всегда перехватывайте:**
- Ошибки сети (timeout, connection)
- HTTP ошибки (4xx, 5xx)
- Ошибки парсинга JSON

```python
try:
    response = requests.get(url, timeout=10)
    response.raise_for_status()
    data = response.json()
except requests.exceptions.Timeout:
    # Обработка таймаута
except requests.exceptions.HTTPError:
    # Обработка HTTP ошибок
except requests.exceptions.RequestException:
    # Общая обработка ошибок
```

### Асинхронность

**Важное правило:**
- Если используете асинхронность — используйте ее ВЕЗДЕ
- ❌ Нельзя смешивать sync и async функции
- Могут быть недостоверные данные, обрывы, неправильная последовательность

**Если нужна асинхронность:**
```python
import asyncio
import aiohttp

async def get_weather_async(city):
    async with aiohttp.ClientSession() as session:
        async with session.get(url) as response:
            return await response.json()
```

### Улучшения проекта

**Дополнительные функции:**
- Прогноз на несколько дней
- Графики температуры
- Сохранение истории запросов в БД
- Аналитика использования бота
- Уведомления о погоде

**База данных:**
- Подключить через SQLAlchemy ORM
- Записывать: пользователя, запрос, время, город
- Собирать аналитику: частота использования, популярные города

**Расширенная функциональность:**
- Направление ветра (конвертация градусов в направления)
- Время восхода/заката солнца
- Атмосферное давление
- Видимость

---

## 11. Полезные ссылки и ресурсы

### API для практики
- OpenWeatherMap: https://openweathermap.org/api
- JSONPlaceholder: https://jsonplaceholder.typicode.com (тестовый API)
- RandomUser API: https://randomuser.me/api
- CoinGecko API: https://www.coingecko.com/en/api (криптовалюты)

### Инструменты
- **Postman** — тестирование API запросов
- **Thunder Client** — расширение VS Code для тестирования API
- **curl** — командная строка для HTTP запросов

### Библиотеки Python
- `requests` — синхронные HTTP запросы
- `aiohttp` — асинхронные HTTP запросы
- `pyTelegramBotAPI` (telebot) — Telegram боты
- `python-dotenv` — работа с .env файлами

---

## 12. Ключевые выводы

✅ **API — это посредник** между клиентом и сервером

✅ **REST API** — самый популярный тип (простой, быстрый, гибкий)

✅ **HTTP методы:** GET (чтение), POST (создание), PATCH/PUT (обновление), DELETE (удаление)

✅ **Статус-коды:** 2xx (успех), 4xx (ошибка клиента), 5xx (ошибка сервера)

✅ **Безопасность:** всегда используйте .env для токенов и ключей

✅ **Обработка ошибок:** критически важна при работе с API

✅ **Telegram Bot** — удобная оболочка для взаимодействия с пользователями

✅ **Асинхронность:** или везде, или нигде (не смешивать!)

✅ На собеседованиях часто спрашивают различия между REST, SOAP и GraphQL

---

## Домашнее задание

Доработать проект Telegram бота с погодой:
- Добавить прогноз на несколько дней
- Реализовать сохранение запросов в БД
- Добавить дополнительные параметры погоды (ветер, давление)
- Улучшить обработку ошибок
- Добавить логирование действий пользователей