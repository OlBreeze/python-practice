# 🎯 Коли використовувати який спосіб - Короткий гайд

## 📊 Порівняльна таблиця

| Спосіб | Складність | Швидкість | Надійність | Коли використовувати |
|--------|-----------|-----------|-----------|---------------------|
| **EventBus (в пам'яті)** | ⭐ | ⚡⚡⚡ | ⭐ | Один процес, прості задачі |
| **Queue (черга)** | ⭐⭐ | ⚡⚡ | ⭐⭐ | Асинхронна обробка, захист від падінь |
| **Event Replay** | ⭐⭐ | ⚡ | ⭐⭐⭐ | Аудит, відновлення стану |
| **RabbitMQ** | ⭐⭐⭐ | ⚡⚡ | ⭐⭐⭐⭐ | Кілька сервісів, продакшн |
| **Webhook** | ⭐⭐ | ⚡⚡ | ⭐⭐⭐ | Інтеграція з зовнішніми API |
| **File Kafka** | ⭐⭐⭐⭐ | ⚡⚡⚡ | ⭐⭐⭐⭐⭐ | Великий обсяг даних, replay |
| **Saga Pattern** | ⭐⭐⭐⭐⭐ | ⚡ | ⭐⭐⭐⭐⭐ | Розподілені транзакції |

---

## 1️⃣ **EventBus (в пам'яті)** - Найпростіший

### 📝 Що це:
Події зберігаються в пам'яті, listener-и викликаються синхронно.

### ✅ Використовуй коли:
- **Один процес/сервіс** (не мікросервіси)
- **Прості задачі** - внутрішня логіка додатку
- **Не критично** якщо події загубляться при перезапуску
- **Швидкість важлива** - всередині одного додатку

### ❌ НЕ використовуй коли:
- Потрібна надійність (при падінні все загубиться)
- Кілька серверів/процесів
- Важкі операції (блокує основний потік)

### 💡 Приклад:
```python
# UI додаток - користувач натиснув кнопку
bus.emit("button.clicked", {"button_id": "submit"})
  → update_ui()
  → log_analytics()
  → show_notification()
```

**Аналогія:** Як дзвінок у офісі - всі в кімнаті чують одразу.

---

## 2️⃣ **Queue (черга)** - Асинхронна обробка

### 📝 Що це:
Події кладуться в чергу, окремий worker обробляє у фоні.

### ✅ Використовуй коли:
- **Важкі операції** (відправка email, обробка зображень)
- **Не хочеш блокувати** основний потік
- **Захист від помилок** - один listener падає, інші працюють
- **Один сервер**, але потрібна асинхронність

### ❌ НЕ використовуй коли:
- Потрібна миттєва реакція (UI updates)
- Дуже проста логіка (overhead не виправданий)
- Кілька серверів (черга в пам'яті не поділиться)

### 💡 Приклад:
```python
# Користувач зареєструвався
bus.emit("user.registered", {"email": "user@example.com"})
# Відразу повертаємо "OK" користувачу
# Worker у фоні:
  → send_welcome_email() # повільно, 3 секунди
  → create_profile_pic() # повільно, 5 секунд
```

**Аналогія:** Як замовлення в ресторані - офіціант приймає, кухня готує паралельно.

---

## 3️⃣ **Event Replay** - Історія подій

### 📝 Що це:
Всі події записуються у файл, можна "перемотати" історію.

### ✅ Використовуй коли:
- **Аудит** - потрібна історія всіх змін (банки, медицина)
- **Відновлення стану** - rebuild даних з подій
- **Debugging** - "що пішло не так о 14:35?"
- **Event Sourcing** - події = джерело істини

### ❌ НЕ використовуй коли:
- Проста CRUD система (зайва складність)
- Дуже багато подій (файли розростуться)
- Не потрібна історія

### 💡 Приклад:
```python
# Банківські транзакції
events.log:
  {"event": "deposit", "amount": 1000}
  {"event": "withdraw", "amount": 200}
  {"event": "deposit", "amount": 500}
  
# Можна replay → баланс = 1300
# Можна побачити всю історію операцій
```

**Аналогія:** Як журнал у бухгалтерії - кожна операція записана, можна перерахувати баланс.

---

## 4️⃣ **RabbitMQ** - Продакшн черга

### 📝 Що це:
Окремий сервер для черг повідомлень між сервісами.

### ✅ Використовуй коли:
- **Мікросервіси** - кілька незалежних сервісів
- **Продакшн** - потрібна надійність
- **Масштабування** - додаєш worker-ів на інших серверах
- **Гарантія доставки** - RabbitMQ зберігає повідомлення

### ❌ НЕ використовуй коли:
- Проста система на одному сервері
- Немає DevOps навичок (треба налаштовувати)
- Мала команда (складно підтримувати)

### 💡 Приклад:
```
Service 1 (Python) → RabbitMQ → Service 2 (Node.js)
                              → Service 3 (Java)
                              → Service 4 (Python)

# Замовлення створено
Order Service → RabbitMQ
                  ↓
    ┌─────────────┼─────────────┐
    ↓             ↓             ↓
Email Worker  SMS Worker  Analytics
```

**Аналогія:** Як пошта - відправник не знає хто отримає, пошта гарантує доставку.

---

## 5️⃣ **Webhook** - Інтеграція з зовнішніми сервісами

### 📝 Що це:
Зовнішній сервіс надсилає HTTP POST на твій сервер.

### ✅ Використовуй коли:
- **Інтеграція** з GitHub, Stripe, LiqPay, PayPal
- **Реал-тайм сповіщення** від зовнішніх систем
- **Event-driven з зовні** - не ти опитуєш, тобі надсилають

### ❌ НЕ використовуй коли:
- Внутрішня комунікація між твоїми сервісами
- Потрібен request-response (краще REST)

### 💡 Приклад:
```python
# Stripe надсилає webhook коли користувач оплатив
POST /webhook/payment
{
  "event": "payment.success",
  "amount": 1500,
  "customer_id": 123
}

Твій сервер:
  → створює замовлення
  → відправляє email
  → оновлює підписку
```

**Аналогія:** Як SMS повідомлення від банку - ти не запитував, але банк повідомив про операцію.

---

## 6️⃣ **File Kafka** - Великі об'єми даних

### 📝 Що це:
Append-only лог файл, consumer-и читають з offset-у.

### ✅ Використовуй коли:
- **Величезний об'єм подій** (мільйони на день)
- **Потрібен replay** - перечитати історію
- **Кілька consumer-ів** читають незалежно
- **Stream processing** - обробка потоку даних

### ❌ НЕ використовуй коли:
- Малий об'єм даних (зайва складність)
- Потрібен request-response
- Просте CRUD додаток

### 💡 Приклад:
```python
# IoT сенсори надсилають дані
orders.log (append-only):
  {"temp": 22.5, "timestamp": "..."}
  {"temp": 22.7, "timestamp": "..."}
  {"temp": 22.6, "timestamp": "..."}
  ... мільйони записів

Consumer 1 (offset: 1000) → обчислює середню температуру
Consumer 2 (offset: 500) → виявляє аномалії
Consumer 3 (offset: 0) → створює звіти за весь час
```

**Аналогія:** Як стрічка відеореєстратора - записується постійно, можна переглянути будь-який момент.

---

## 7️⃣ **Saga Pattern** - Розподілені транзакції

### 📝 Що це:
Послідовність кроків, кожен з яких можна відкотити (компенсувати).

### ✅ Використовуй коли:
- **Мікросервіси** з різними БД
- **Складний бізнес-процес** (замовлення → оплата → доставка)
- **Потрібна консистентність** між сервісами
- **Не можна використовувати** розподілену транзакцію (2PC)

### ❌ НЕ використовуй коли:
- Одна БД (використовуй звичайні транзакції)
- Простий процес (зайва складність)
- Малий проєкт

### 💡 Приклад:
```python
# Бронювання подорожі
Крок 1: Забронювати готель ✅
Крок 2: Купити квиток ✅
Крок 3: Забронювати трансфер ❌ ПОМИЛКА

Компенсація:
  ← Скасувати квиток
  ← Скасувати готель

Результат: ВСЕ або НІЧОГО
```

**Аналогія:** Як домінові кістки - якщо одна впала, відкочуємо всі назад.

---

## 🎯 Практичні сценарії

### 🛒 **E-commerce (інтернет-магазин)**

```
1. Користувач створює замовлення
   → EventBus (синхронно) - оновити UI

2. Обробка замовлення
   → Queue - відправити email, SMS (асинхронно)
   → Event Replay - зберегти історію замовлень

3. Взаємодія сервісів
   → RabbitMQ між Order, Payment, Delivery сервісами

4. Платіжна система
   → Webhook від Stripe/LiqPay

5. Якщо багато замовлень
   → File Kafka для аналітики

6. Процес оформлення
   → Saga (резерв товару → оплата → доставка)
```

---

### 📱 **Соціальна мережа**

```
1. Новий пост
   → EventBus - оновити фід в UI

2. Розсилка підписникам
   → Queue - push notifications

3. Аналітика активності
   → File Kafka - stream processing

4. Інтеграція з іншими платформами
   → Webhook від Facebook/Instagram
```

---

### 🏦 **Банківський додаток**

```
1. Транзакція
   → Event Replay - повна історія (аудит)

2. Переказ між банками
   → Saga Pattern - якщо помилка, відкотити

3. Fraud detection
   → File Kafka - аналіз потоку транзакцій

4. Сповіщення
   → Queue - SMS про операцію
```

---

## 🚦 Швидкий вибір

### Задай собі питання:

1. **Скільки сервісів?**
   - 1 сервіс → EventBus або Queue
   - 2-5 сервісів → RabbitMQ
   - 5+ сервісів → Kafka

2. **Потрібна історія?**
   - Ні → EventBus/Queue
   - Так → Event Replay або Kafka

3. **Зовнішні інтеграції?**
   - Так → Webhook

4. **Розподілена транзакція?**
   - Так → Saga Pattern

5. **Об'єм даних?**
   - < 1000/день → EventBus/Queue
   - 1K-100K/день → RabbitMQ
   - > 100K/день → Kafka

---

## 🎓 Еволюція проєкту

```
Стадія 1 (MVP):
EventBus в пам'яті ✅

↓

Стадія 2 (Зростання):
+ Queue для важких операцій ✅

↓

Стадія 3 (Масштабування):
+ RabbitMQ між сервісами ✅
+ Webhook для інтеграцій ✅

↓

Стадія 4 (Великий бізнес):
+ Kafka для великих даних ✅
+ Event Replay для аудиту ✅
+ Saga для складних процесів ✅
```

**Висновок:** Починай просто, ускладнюй коли потрібно! 🚀