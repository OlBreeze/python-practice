Ось твоє завдання, оформлене у **Markdown**:

---

# 📘 Завдання з Django (сесії, cookies, ORM, кешування, продуктивність, Celery, NoSQL)

## 1. **Управління сесіями та cookies**

Створити Django-застосунок з формою входу, де:

* користувач вводить **ім'я** та **вік**;
* **ім'я** зберігається у *cookies*, а **вік** — у *сесії*.

### 🎯 Реалізувати:

#### 1.1. Сторінка входу

* форма з полями: `name`, `age`;
* після надсилання:

  * ім'я -> cookies;
  * вік -> session.

#### 1.2. Сторінка привітання

* використовує дані з cookies та session
  *Наприклад: **"Привіт, {ім'я}, вам {вік} років"***
* містить кнопку **"Вийти"**, яка:

  * очищає cookies;
  * очищає сесію.

#### 1.3. Додаткові вимоги

* перевірка, чи не протермінувалися cookies;
* автоподовження cookies при активності користувача;
* (опціонально) зберігання сесій у базі даних через `django.contrib.sessions`.

---

## 2. **Оптимізація та продуктивність**

### 2.1. Оптимізація запитів ORM

#### Створити моделі:

* `Author`
* `Book`
* `Review`

Звʼязки:

* один Author → багато Book
* одна Book → багато Review

#### Завдання:

1. Наповнити БД ≥ 50 записів.
2. Написати два варіанти запитів:

   * без оптимізації;
   * з `select_related` / `prefetch_related`.
3. Виміряти та порівняти час виконання.

---

### 2.2. Кешування

Створити подання, яке:

* показує список книг із авторами;
* кешує результат через Django cache.

Додатково:

* оновлювати кеш при додаванні/редагуванні книги;
* налаштувати кеш у Memcached або Redis;
* написати middleware, яке кешує сторінку списку книг для анонімних користувачів.

---

### 2.3. Аналіз продуктивності з django-debug-toolbar

1. Встановити `django-debug-toolbar`.
2. Перевірити подання списку книг.
3. Знайти “вузькі місця” — зайві SQL-запити, неоптимальні шаблони.
4. Оптимізувати.

---

### 2.4. Оптимізація шаблонів

* Використати шаблони для відображення:

  * книг,
  * авторів,
  * відгуків.
* Переконатися, що дані передаються оптимізованими ORM-запитами.
* Перевірити продуктивність через debug-toolbar.

---

### 2.5. Асинхронні завдання з Celery

1. Встановити й налаштувати **Celery + Redis**.
2. Створити завдання, що:

   * імпортує книги з CSV;
   * зберігає у БД;
   * надсилає email про завершення.
3. Реалізувати сторінку стану завдання.

---

## 3. **Глибоке занурення в ORM**

### 3.1. Анотації та агрегації

Реалізувати:

* середній рейтинг книг кожного автора;
* кількість відгуків для кожної книги;
* сортування за:

  * кількістю відгуків,
  * середнім рейтингом.

Зробити сторінку з таблицею цих даних.

---

### 3.2. Raw SQL

Створити відображення, що виконує сирий SQL:

1. Вибір авторів, у яких є книги з > 10 відгуками.
2. Підрахунок загальної кількості книг.

Обовʼязково:

* захистити від SQL-інʼєкцій (параметризовані запити).

---

### 3.3. Індексація та оптимізація

* Додати індекси в моделі Book/Review (наприклад, поле `rating`);
* Порівняти час запитів:

  * до індексації,
  * після індексації.

---

### 3.4. Підтримка NoSQL

* Налаштувати Django для роботи з **MongoDB** або **Firebase**;
* Реалізувати:

  * запис даних,
  * читання списку книг із NoSQL.
* Порівняти продуктивність із реляційною БД.

---

## 📤 Результати, які треба надати:

1. Код проєкту + SQL-запити → **GitHub**.
2. Скріншоти:

   * django-debug-toolbar;
   * час виконання запитів;
   * Celery task results.
3. Короткий звіт про виконання.

---

Якщо хочеш, можу підготувати **структуру проєкту**, **приклади моделей**, **urls**, **views**, або допомогти з будь-яким підпунктом.
