## Практические задания 
1. **Создать кастомный фильтр** для шаблонов (например, форматирование даты) 
2. **Создать кастомный тег** для шаблонов (например, вывод текущего времени) 
3. **Создать контекстный процессор** (например, информация о сайте) 
4. **Применить все три** в одном шаблоне  
 
Эти практические задания — про **темплейтные фильтры, теги и контекстные процессоры** Django.
Они позволяют **добавлять в шаблоны свою собственную логику**.
Давай я покажу, как сделать каждое из них и объединить в одном шаблоне — по шагам 👇
                    

---

## ✅ 1. Кастомный фильтр для шаблонов (пример: форматирование даты)

📁 Создай файл `templatetags/custom_filters.py` в твоём приложении (например, `main/templatetags/custom_filters.py`):

```python
from django import template
from datetime import datetime

register = template.Library()

@register.filter
def format_date(value):
    """Форматировать дату в красивый вид"""
    if isinstance(value, datetime):
        return value.strftime("%d.%m.%Y %H:%M")
    return value
```

> ⚠️ Важно: папка `templatetags` должна содержать `__init__.py`, чтобы Django её распознал.

📘 Использование в шаблоне:

```django
{{ some_date|format_date }}
```

---

## ✅ 2. Кастомный тег (пример: вывод текущего времени)

В том же каталоге `templatetags/`, создадим файл `custom_tags.py`:

```python
from django import template
from datetime import datetime

register = template.Library()

@register.simple_tag
def current_time(format_string="%H:%M:%S"):
    """Выводит текущее время"""
    return datetime.now().strftime(format_string)
```

📘 Использование в шаблоне:

```django
{% load custom_tags %}
<p>Сейчас: {% current_time "%d.%m.%Y %H:%M:%S" %}</p>
```

---

## ✅ 3. Контекстный процессор (пример: информация о сайте)

📁 Создай файл `context_processors.py` в твоём приложении (например, `main/context_processors.py`):

```python
def site_info(request):
    """Добавляет информацию о сайте в каждый шаблон"""
    return {
        'site_name': 'My Awesome Django Site',
        'author': 'Olga Golovash',
    }
```

Теперь подключи его в `settings.py`:

```python
TEMPLATES = [
    {
        ...
        'OPTIONS': {
            'context_processors': [
                ...
                'main.context_processors.site_info',  # добавь сюда
            ],
        },
    },
]
```

Теперь переменные `site_name` и `author` будут доступны **во всех шаблонах**.

---

## ✅ 4. Применяем всё в одном шаблоне

📁 Пример шаблона `templates/home.html`:

```django
{% load custom_filters custom_tags %}

<h1>Добро пожаловать на {{ site_name }}</h1>
<p>Автор сайта: {{ author }}</p>

<p>Дата создания страницы: {{ some_date|format_date }}</p>

<p>Текущее время: {% current_time "%H:%M:%S" %}</p>
```

📘 А во `views.py`:

```python
from django.shortcuts import render
from datetime import datetime

def home(request):
    return render(request, 'home.html', {'some_date': datetime(2025, 11, 12, 10, 0)})
```

---

## 🧭 Результат в браузере:

```
Добро пожаловать на My Awesome Django Site
Автор сайта: Olga Golovash
Дата создания страницы: 12.11.2025 10:00
Текущее время: 10:45:37
```

---
