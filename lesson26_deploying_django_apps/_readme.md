# Развертывание Django-приложений на PythonAnywhere

## Содержание
1. [Введение в деплоймент](#введение)
2. [Основные понятия](#основные-понятия)
3. [Подготовка к развертыванию](#подготовка)
4. [PythonAnywhere: обзор платформы](#pythonanywhere)
5. [Пошаговое развертывание](#пошаговое-развертывание)
6. [Настройка settings.py](#настройка-settings)
7. [Работа с WSGI](#работа-с-wsgi)
8. [Статические файлы и медиа](#статические-файлы)
9. [Безопасность](#безопасность)
10. [Troubleshooting](#troubleshooting)

---

## Введение

**Деплоймент (Deployment)** — это процесс переноса веб-приложения с локальной машины разработчика на удаленный сервер, где оно становится доступным пользователям через интернет.

### Зачем нужен деплоймент?

- **Доступность**: приложение становится доступным из любой точки мира
- **Тестирование в реальных условиях**: проверка работы в production-среде
- **Демонстрация заказчику**: возможность показать работающий проект
- **Публичное использование**: запуск коммерческого продукта

---

## Основные понятия

### Хостинг (Hosting)
**Хостинг** — это сервис, предоставляющий физический сервер и инфраструктуру для размещения вашего приложения.

**Типы хостинга:**
- **Shared Hosting** — общий сервер для множества проектов
- **VPS (Virtual Private Server)** — виртуальный выделенный сервер
- **Dedicated Server** — физический выделенный сервер
- **Cloud Hosting** — облачные решения (AWS, Azure, Google Cloud)
- **PaaS (Platform as a Service)** — платформа с готовой инфраструктурой (Heroku, PythonAnywhere)

### DNS (Domain Name System)
**DNS** — система доменных имен, которая преобразует удобочитаемые доменные имена (например, `google.com`) в IP-адреса серверов.

**Преимущества:**
- Легче запомнить `mysite.com`, чем `192.168.1.1`
- Возможность перенести сайт на другой сервер без изменения адреса
- Профессиональный вид проекта

### Production (Продакшн)
**Production** — это рабочая среда, где приложение используется реальными пользователями.

**Отличия от разработки:**
- `DEBUG = False` — отключен режим отладки
- Скрыты чувствительные данные (SECRET_KEY, пароли БД)
- Оптимизация производительности
- Настроена система логирования
- Резервное копирование данных

### WSGI (Web Server Gateway Interface)
**WSGI** — стандартный интерфейс между веб-сервером и Python-приложением.

Вот **упрощённая схема работы WSGI** в виде блок-схемы и последовательности шагов.

---

#### 🧩 **Блок-схема**

```
┌──────────────┐
│   Клиент     │
│ (браузер)    │
└──────┬───────┘
       │ HTTP-запрос
       ▼
┌──────────────┐
│  Веб-сервер  │
│ (Nginx/Apach)│
└──────┬───────┘
       │ Проксирование
       ▼
┌──────────────┐
│ WSGI-сервер  │
│ (Gunicorn/   │
│   uWSGI)     │
└──────┬───────┘
       │ Вызов WSGI-приложения
       ▼
┌──────────────┐
│  Python/     │
│ Django/Flask │
│  приложение  │
└──────┬───────┘
       │ Формирование ответа
       ▼
┌──────────────┐
│ WSGI-сервер  │
└──────┬───────┘
       │ Отправка назад
       ▼
┌──────────────┐
│  Веб-сервер  │
└──────┬───────┘
       │ HTTP-ответ
       ▼
┌──────────────┐
│   Клиент     │
└──────────────┘
```

---

#### 📌 **Пошагово**

1. **Браузер отправляет HTTP-запрос** на сервер.
2. **Веб-сервер (Nginx/Apache)** принимает HTTP-запрос.
3. Если запрос статический — Nginx отдаёт файл сам.
   Если динамический — пересылает на WSGI-сервер(Gunicorn, uWSGI).
4. **WSGI-сервер (Gunicorn/uWSGI)** получает запрос и:
   * создаёт окружение (`environ`)
   * вызывает Python-приложение через WSGI-интерфейс.
5. **Django/Flask** обрабатывает запрос:
   * маршрутизация
   * бизнес-логика
   * запросы к БД
   * формирование ответа
6. Приложение возвращает данные WSGI-серверу.
7. WSGI-сервер передаёт ответ обратно веб-серверу.
8. **Веб-сервер** отправляет HTTP-ответ клиенту.
---

### ⚡ * Дополнительная информация чем **WSGI** отличается от **ASGI (Asynchronous Server Gateway Interface)**


#### 🧩 1. **Тип обработки запросов**

##### **WSGI — синхронный** (Web Server Gateway Interface)

* Работает **строго по одному запросу на поток**.
* Подходит для классического HTTP.
* Нельзя эффективно обрабатывать WebSocket, long polling, SSE.

##### **ASGI — асинхронный** (Asynchronous Server Gateway Interface)

* Поддерживает **многозадачность в одном потоке** через async/await.
* Умеет работать с:

  * WebSocket (см. ниже лекции описание работы)
  * long-polling
  * очередями событий
  * стримингом
  * фоновой работой

---

#### 🚀 2. **Где применяется**

##### **WSGI**

Используют:

* Django (до версии 3 как основной вариант)
* Flask
* Pyramid
* Bottle

Проекты: обычные сайты, REST API, админки.

##### **ASGI**

Используют:

* Django 3+ (частично — асинхронные views, но WebSocket через ASGI)
* FastAPI
* Starlette
* Quart
* Channels

Проекты: real-time приложения, чат, игры, стримы, API высокой нагрузки.

---

#### 🌐 3. **Что умеют**

| Возможность             | WSGI    | ASGI    |
| ----------------------- | ------- | ------- |
| HTTP                    | ✓       | ✓       |
| WebSockets              | ✗       | ✓       |
| SSE                     | ✗       | ✓       |
| Асинхронный код         | ✗       | ✓       |
| Синхронный код          | ✓       | ✓       |
| Горизонтальная нагрузка | средняя | высокая |

---

#### ⚙ 4. **Архитектура**

##### **WSGI**

Одно приложение — один протокол:

```
web server → WSGI → приложение
```

##### **ASGI**

Многоуровневая система событий:

```
web server → ASGI server → event loop → приложение
```

ASGI сохраняет совместимость с WSGI через адаптер.

---

#### ⏳ 5. Почему появился ASGI?

Потому что современным вебу нужны функции реального времени, а WSGI ограничен синхронной моделью.

Коротко: **если ты деплоишь обычное синхронное Python-приложение (Django, Flask) — ты деплоишь через WSGI.**
Если асинхронное (FastAPI, Starlette, WebSocket, async Django) — тогда через **ASGI**.

Но давай точнее.

---

### ✅ Если мы говорим о деплое — это WSGI?

**В большинстве классических случаев — да.**
Под "деплоем Python-приложения" обычно подразумевают:

* Nginx
* Gunicorn / uWSGI
* Django / Flask
* systemd service

И это **WSGI-цепочка**.

То есть:
**Деплой Django/Flask → почти всегда WSGI.**

---

#### ⚠️ Но не всегда.

Сегодня всё чаще деплоят приложения, использующие:

* FastAPI
* WebSockets
* асинхронные Django views
* фоновые задачи на asyncio
* real-time сервисы

Это всё **ASGI**.

Типичная ASGI-цепочка:

* Nginx
* Uvicorn / Hypercorn / Daphne
* FastAPI / Django ASGI

---

### 🧭 Как определить, что именно у тебя?

 ✔ **Если у тебя в проекте есть файл**

`wsgi.py` → это WSGI
`asgi.py` → это ASGI

В Django оба файла создаются, но:

* Если запускаешь `gunicorn myproject.wsgi`, → WSGI
* Если запускаешь `uvicorn myproject.asgi`, → ASGI

---

#### 🧠 Итог

**Говорим “деплой Python сайта” → обычно подразумеваем WSGI.**
Но **современные** (async) приложения → это уже **ASGI**.

| WSGI                  | ASGI                              |
| --------------------- | --------------------------------- |
| Синхронный            | Асинхронный                       |
| Только HTTP           | HTTP + WebSocket + фоновые задачи |
| Хорош для классики    | Хорош для real-time и high-load   |
| Простая модель работы | Сложнее, но мощнее                |

---

### SSL/TLS сертификат
**SSL (Secure Sockets Layer)** — протокол шифрования данных между клиентом и сервером.

**Что дает:**
- Защита данных от перехвата
- HTTPS вместо HTTP
- Повышение доверия пользователей
- Улучшение SEO (Google отдает предпочтение HTTPS-сайтам)

---

## Подготовка к развертыванию

### Структура типичного проекта Django

```
myproject/
├── myproject/           # Конфигурационная директория
│   ├── __init__.py
│   ├── settings.py     # Настройки проекта
│   ├── urls.py         # Главный маршрутизатор
│   ├── wsgi.py         # WSGI-конфигурация
│   └── asgi.py         # ASGI-конфигурация (для асинхронных приложений)
├── app1/               # Приложение 1
│   ├── migrations/
│   ├── models.py
│   ├── views.py
│   ├── urls.py
│   └── templates/
├── static/             # Статические файлы (CSS, JS, изображения)
├── media/              # Загружаемые пользователями файлы
├── templates/          # Глобальные шаблоны
├── manage.py           # Утилита управления Django
├── requirements.txt    # Список зависимостей
├── .env               # Переменные окружения (НЕ публикуется в Git)
└── db.sqlite3         # База данных (для разработки)
```

### Критические настройки settings.py

#### 1. DEBUG
```python
# НЕ ИСПОЛЬЗУЙТЕ DEBUG = True В PRODUCTION!
DEBUG = False  # Отключает подробные сообщения об ошибках
```

**Почему это важно:**
- В режиме `DEBUG = True` отображается детальная информация об ошибках, включая:
  - Код приложения
  - Переменные окружения
  - Пути к файлам
  - Запросы к БД
- Это критическая брешь в безопасности!

#### 2. ALLOWED_HOSTS
```python
ALLOWED_HOSTS = [
    'yourusername.pythonanywhere.com',  # Ваш домен на PythonAnywhere
    'www.yourdomain.com',                # Кастомный домен
    'yourdomain.com',
]
```

**Назначение:**
- Защита от HTTP Host header attacks
- Django проверяет заголовок `Host` в запросе
- Если домен не в списке, возвращается ошибка 400 Bad Request

#### 3. SECRET_KEY
```python
# ❌ ПЛОХО - ключ в коде
SECRET_KEY = 'django-insecure-key-123456'

# ✅ ХОРОШО - ключ в переменных окружения
import os
SECRET_KEY = os.environ.get('DJANGO_SECRET_KEY', 'fallback-key-for-dev')
```

**Использование .env файла:**
```python
# settings.py
from dotenv import load_dotenv
import os

load_dotenv()

SECRET_KEY = os.getenv('SECRET_KEY')
DEBUG = os.getenv('DEBUG', 'False') == 'True'
```

**.env файл:**
```
SECRET_KEY=your-secret-key-here
DEBUG=False
DATABASE_URL=postgresql://user:password@localhost/dbname
```

**Добавьте .env в .gitignore:**
```
# .gitignore
.env
*.pyc
__pycache__/
db.sqlite3
/static/
/media/
```

#### 4. Статические файлы
```python
STATIC_URL = '/static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'  # Где collectstatic соберет файлы

STATICFILES_DIRS = [
    BASE_DIR / 'static',  # Дополнительные директории со статикой
]
```

#### 5. Медиа файлы
```python
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'
```

### Создание requirements.txt

**Вариант 1: Автоматическая генерация**
```bash
pip freeze > requirements.txt
```

**Вариант 2: Ручное создание (рекомендуется)**
```txt
Django==5.0.6
Pillow==10.3.0
python-dotenv==1.0.0
gunicorn==22.0.0
psycopg2-binary==2.9.9  # Для PostgreSQL
```

**Установка зависимостей:**
```bash
pip install -r requirements.txt
```

---

## PythonAnywhere: обзор платформы

### Что такое PythonAnywhere?

PythonAnywhere — это PaaS-платформа, специально оптимизированная для Python-приложений, которая значительно упрощает процесс деплоя по сравнению с другими хостинг-провайдерами.

### Тарифные планы (2025)

| Тариф | Цена | Особенности |
|-------|------|-------------|
| **Beginner** | Бесплатно (3 месяца) | 1 приложение, SQLite, 100 сек CPU/день, базовый домен |
| **Hacker** | $5/мес | 1 приложение, PostgreSQL/MySQL, 2000 сек CPU/день, кастомный домен |
| **Boosted** | $12/мес | 2 приложения, больше ресурсов, приоритетная поддержка |
| **Web Developer** | $90/мес | До 20 приложений, выделенные ресурсы |

### Преимущества PythonAnywhere

✅ **Простота:**
- Не нужно настраивать веб-сервер
- Готовое окружение Python
- Графический интерфейс для управления

✅ **Бесплатный тариф:**
- Идеально для обучения и портфолио
- Достаточно для небольших проектов

✅ **Встроенные инструменты:**
- Bash-консоль
- Редактор файлов
- Управление базами данных
- Логи и мониторинг

✅ **Автоматический HTTPS:**
- Бесплатный SSL-сертификат для всех доменов
- Защищенное соединение из коробки

### Ограничения бесплатного тарифа

⚠️ **Ресурсы:**
- 100 секунд CPU в день (не хватит для высоконагруженных приложений)
- 512 MB RAM
- 512 MB дискового пространства

⚠️ **База данных:**
- Только SQLite (нет PostgreSQL/MySQL)

⚠️ **Домен:**
- Только `username.pythonanywhere.com`
- Нельзя использовать кастомный домен

⚠️ **Доступность:**
- После 3 месяцев нужно продлевать вручную (за неделю до окончания приходит email)

---

## Пошаговое развертывание

### Шаг 1: Регистрация на PythonAnywhere

1. Перейдите на [https://www.pythonanywhere.com](https://www.pythonanywhere.com)
2. Нажмите "Start running Python online in less than a minute!"
3. Выберите **"Create a Beginner account"**
4. Заполните форму регистрации
5. Подтвердите email

### Шаг 2: Подготовка проекта на локальной машине

#### 2.1 Обновите settings.py

```python
# settings.py

# Отключите DEBUG
DEBUG = False

# Добавьте домен PythonAnywhere
ALLOWED_HOSTS = ['yourusername.pythonanywhere.com']

# Настройте статические файлы
STATIC_URL = '/static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'

# Настройте медиа-файлы
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'
```

#### 2.2 Создайте requirements.txt

```bash
pip freeze > requirements.txt
```

#### 2.3 Загрузите проект на GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/username/project.git
git push -u origin main
```

### Шаг 3: Клонирование проекта на PythonAnywhere

1. Откройте **Consoles** → **Bash**
2. Клонируйте репозиторий:

```bash
git clone https://github.com/username/project.git
cd project
```

3. Проверьте содержимое:

```bash
ls -la  # Список файлов
pwd     # Текущий путь (скопируйте его!)
```

**Пример вывода:**
```
/home/yourusername/project
```

### Шаг 4: Создание виртуального окружения

На PythonAnywhere используется специальная команда для создания виртуального окружения:

```bash
mkvirtualenv --python=/usr/bin/python3.10 myenv
```

**Параметры:**
- `--python=/usr/bin/python3.10` — версия Python (3.10, 3.11, 3.12, 3.13)
- `myenv` — имя виртуального окружения

**Активация виртуального окружения:**
```bash
workon myenv
```

**Деактивация:**
```bash
deactivate
```

### Шаг 5: Установка зависимостей

```bash
# Убедитесь, что виртуальное окружение активно
workon myenv

# Установите зависимости
pip install -r requirements.txt
```

**Типичные зависимости для Django:**
```bash
pip install django pillow
```

### Шаг 6: Настройка Web App

1. Перейдите на вкладку **Web**
2. Нажмите **"Add a new web app"**
3. Нажмите **"Next"** (домен будет `yourusername.pythonanywhere.com`)
4. Выберите **"Manual configuration"** (НЕ "Django" — это только для новых проектов!)
5. Выберите версию Python (например, Python 3.10)
6. Нажмите **"Next"**

### Шаг 7: Конфигурация виртуального окружения

На вкладке **Web** найдите секцию **"Virtualenv"**:

1. Нажмите на ссылку "Enter path to a virtualenv"
2. Введите путь к виртуальному окружению:

```
/home/yourusername/.virtualenvs/myenv
```

**Как найти точный путь:**
```bash
workon myenv
echo $VIRTUAL_ENV
```

### Шаг 8: Настройка путей к коду

На вкладке **Web** в секции **"Code"**:

#### Source code:
```
/home/yourusername/project
```

#### Working directory:
```
/home/yourusername/project
```

**Важно:** Это директория, где находится `manage.py`!

---

## Работа с WSGI

### Что такое WSGI-файл?

WSGI-файл на PythonAnywhere — это специальный конфигурационный файл, который указывает системе, как запустить ваше Django-приложение. Это НЕ файл `wsgi.py` внутри вашего проекта.

### Редактирование WSGI-файла

1. На вкладке **Web** найдите секцию **"Code"**
2. Кликните на ссылку **"WSGI configuration file"**
3. Откроется редактор

### Шаблон WSGI-файла для Django

```python
# ============================================================
# WSGI configuration file for PythonAnywhere
# ============================================================

import os
import sys

# Добавляем путь к проекту в PYTHONPATH
path = '/home/yourusername/project'
if path not in sys.path:
    sys.path.insert(0, path)

# Указываем, где находится settings.py
os.environ['DJANGO_SETTINGS_MODULE'] = 'myproject.settings'

# Импортируем Django WSGI application
from django.core.wsgi import get_wsgi_application
application = get_wsgi_application()
```

**Замените:**
- `yourusername` → ваш логин на PythonAnywhere
- `project` → имя директории с проектом
- `myproject` → имя папки с `settings.py`

### Типичная структура и соответствующие настройки

**Если структура проекта:**
```
/home/username/mysite/
├── mysite/
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
└── manage.py
```

**WSGI конфигурация:**
```python
path = '/home/username/mysite'
os.environ['DJANGO_SETTINGS_MODULE'] = 'mysite.settings'
```

### Сохранение и перезагрузка

1. Нажмите кнопку **"Save"** (вверху справа)
2. Вернитесь на вкладку **Web**
3. Нажмите большую зеленую кнопку **"Reload yourusername.pythonanywhere.com"**

---

## Настройка settings.py

### Редактирование через Web-интерфейс

1. Перейдите на вкладку **Files**
2. Найдите файл `settings.py`
3. Кликните для редактирования

### Обязательные настройки для production

#### 1. DEBUG и ALLOWED_HOSTS

```python
# settings.py

DEBUG = False

ALLOWED_HOSTS = [
    'yourusername.pythonanywhere.com',
]
```

**Если забыли добавить домен:**
```
Bad Request (400)
```

#### 2. Статические файлы

```python
STATIC_URL = '/static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'

# Опционально: дополнительные директории
STATICFILES_DIRS = [
    BASE_DIR / 'static',
]
```

#### 3. Медиа файлы

```python
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'
```

#### 4. База данных (SQLite)

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}
```

**Для PostgreSQL/MySQL (платные тарифы):**
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'yourusername$dbname',
        'USER': 'yourusername',
        'PASSWORD': os.environ.get('DB_PASSWORD'),
        'HOST': 'yourusername.postgres.pythonanywhere-services.com',
        'PORT': '10000',
    }
}
```

### Миграции базы данных

После настройки выполните миграции через Bash-консоль:

```bash
cd /home/yourusername/project
workon myenv
python manage.py migrate
python manage.py createsuperuser  # Создание админа (опционально)
```

---

## Статические файлы и медиа

### Сбор статических файлов

```bash
python manage.py collectstatic
```

**Что делает эта команда:**
- Собирает все статические файлы из приложений
- Копирует их в директорию `STATIC_ROOT`
- Создает единую точку для обслуживания статики

### Настройка путей на PythonAnywhere

На вкладке **Web** найдите секцию **"Static files"**:

#### Настройка статики:

| URL | Directory |
|-----|-----------|
| `/static/` | `/home/yourusername/project/staticfiles` |

#### Настройка медиа:

| URL | Directory |
|-----|-----------|
| `/media/` | `/home/yourusername/project/media` |

**Пример:**

1. Нажмите "Enter URL" → введите `/static/`
2. Нажмите "Enter path" → введите `/home/yourusername/project/staticfiles`
3. Повторите для `/media/`

### Проверка статических файлов

После настройки перейдите по адресу:
```
https://yourusername.pythonanywhere.com/static/admin/css/base.css
```

Если файл отображается — настройка успешна!

---

## Безопасность

### 1. HTTPS (Force HTTPS)

На вкладке **Web** в секции **"Security"**:

- Включите переключатель **"Force HTTPS"**
- Все HTTP-запросы будут автоматически перенаправлены на HTTPS

### 2. Дополнительная аутентификация (Password protection)

В секции **"Password protection"**:

```
Username: admin
Password: your-secure-password
```

**Когда использовать:**
- Закрытые бета-версии
- Демонстрации для клиента
- Дополнительный уровень защиты

**Как работает:**
1. Пользователь открывает сайт
2. Появляется окно HTTP Basic Authentication
3. Нужно ввести логин и пароль
4. Только после этого открывается сайт

### 3. Скрытие чувствительных данных

#### Использование переменных окружения

**Создание .env файла на PythonAnywhere:**

```bash
cd /home/yourusername/project
nano .env
```

**Содержимое .env:**
```
SECRET_KEY=your-django-secret-key-here
DEBUG=False
DATABASE_URL=sqlite:///db.sqlite3
```

**settings.py:**
```python
from dotenv import load_dotenv
import os

load_dotenv()

SECRET_KEY = os.getenv('SECRET_KEY')
DEBUG = os.getenv('DEBUG', 'False') == 'True'
```

### 4. Безопасные настройки Django

```python
# settings.py

# Дополнительные настройки безопасности для production
SECURE_SSL_REDIRECT = True  # Принудительный HTTPS
SESSION_COOKIE_SECURE = True  # Cookies только через HTTPS
CSRF_COOKIE_SECURE = True  # CSRF токены только через HTTPS

# Защита от clickjacking
X_FRAME_OPTIONS = 'DENY'

# Защита от XSS
SECURE_BROWSER_XSS_FILTER = True
SECURE_CONTENT_TYPE_NOSNIFF = True
```

---

## Troubleshooting

### Ошибка 400 Bad Request

**Причина:** Домен не добавлен в `ALLOWED_HOSTS`

**Решение:**
```python
# settings.py
ALLOWED_HOSTS = ['yourusername.pythonanywhere.com']
```

### Ошибка 500 Internal Server Error

**Причины:**
1. Синтаксическая ошибка в коде
2. Неправильная конфигурация WSGI
3. Отсутствующие зависимости
4. Проблемы с базой данных

**Решение:**
1. Проверьте логи на вкладке **Web**:
   - Error log
   - Server log
   - Access log

2. Проверьте WSGI конфигурацию
3. Убедитесь, что все пакеты установлены:
```bash
pip install -r requirements.txt
```

4. Проверьте миграции:
```bash
python manage.py migrate
```

### Статические файлы не загружаются

**Причины:**
1. Не выполнена команда `collectstatic`
2. Неправильные пути в настройках

**Решение:**
```bash
python manage.py collectstatic --noinput
```

Проверьте пути на вкладке **Web** → **Static files**

### Изменения не применяются

**Причина:** Приложение нужно перезагрузить после изменений

**Решение:**
1. Перейдите на вкладку **Web**
2. Нажмите зеленую кнопку **"Reload"**

### Виртуальное окружение не активируется

**Проблема:**
```bash
mkvirtualenv: command not found
```

**Решение:**
```bash
source virtualenvwrapper.sh
mkvirtualenv --python=/usr/bin/python3.10 myenv
```

### База данных не найдена

**Решение:**
```bash
cd /home/yourusername/project
python manage.py migrate
```

---

## Полный чеклист развертывания

### Подготовка (локально)

- [ ] `DEBUG = False` в settings.py
- [ ] Добавлен домен в `ALLOWED_HOSTS`
- [ ] Создан requirements.txt
- [ ] SECRET_KEY вынесен в переменные окружения
- [ ] Настроены STATIC_ROOT и MEDIA_ROOT
- [ ] Проект загружен на GitHub/GitLab

### На PythonAnywhere

- [ ] Зарегистрирован аккаунт
- [ ] Клонирован проект через git clone
- [ ] Создано виртуальное окружение (`mkvirtualenv`)
- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Создан Web App (Manual configuration)
- [ ] Указан путь к виртуальному окружению
- [ ] Настроены Source code и Working directory
- [ ] Отредактирован WSGI файл
- [ ] Выполнены миграции (`python manage.py migrate`)
- [ ] Собраны статические файлы (`python manage.py collectstatic`)
- [ ] Настроены пути к static и media файлам
- [ ] Включен Force HTTPS
- [ ] Нажата кнопка Reload
- [ ] Проверена работа сайта

---

## Обновление приложения

### Процесс обновления

```bash
# 1. Перейдите в директорию проекта
cd /home/yourusername/project

# 2. Активируйте виртуальное окружение
workon myenv

# 3. Получите последние изменения из Git
git pull origin main

# 4. Установите новые зависимости (если добавлены)
pip install -r requirements.txt

# 5. Выполните миграции (если есть изменения в моделях)
python manage.py migrate

# 6. Соберите статические файлы (если изменились)
python manage.py collectstatic --noinput

# 7. Перезагрузите приложение
# Через Web-интерфейс: Web → Reload
```

---

## Альтернативные платформы для деплоя

### Heroku
- **Плюсы:** Простой деплой через Git, бесплатный тариф (был), хорошая документация
- **Минусы:** Бесплатный тариф отменен, дороже чем PythonAnywhere

### Railway
- **Плюсы:** Современный интерфейс, автоматический деплой из GitHub
- **Минусы:** Ограниченный бесплатный тариф ($5 бесплатно/месяц)

### DigitalOcean App Platform
- **Плюсы:** Надежная инфраструктура, масштабируемость
- **Минусы:** Нет бесплатного тарифа (от $5/месяц)

### Render
- **Плюсы:** Бесплатный тариф, простой деплой
- **Минусы:** Ограниченные ресурсы на бесплатном тарифе

### AWS (Amazon Web Services)
- **Плюсы:** Максимальная гибкость, мощная инфраструктура
- **Минусы:** Сложная настройка, дорого для начинающих

---

## Полезные ссылки

- [PythonAnywhere Documentation](https://help.pythonanywhere.com/)
- [Django Deployment Checklist](https://docs.djangoproject.com/en/stable/howto/deployment/checklist/)
- [Django Settings Best Practices](https://docs.djangoproject.com/en/stable/topics/settings/)
- [WSGI Servers Comparison](https://docs.djangoproject.com/en/stable/howto/deployment/wsgi/)

---

## Заключение

Развертывание Django-приложения на PythonAnywhere — это отличный способ быстро и бесплатно опубликовать свой проект в интернете. Платформа оптимизирована для Python и предоставляет все необходимые инструменты из коробки.

### Ключевые моменты:

1. **Безопасность прежде всего**: всегда отключайте DEBUG и скрывайте секретные ключи
2. **Тестируйте локально**: убедитесь, что проект работает с `DEBUG = False` перед деплоем
3. **Читайте логи**: при возникновении проблем первым делом проверяйте error logs
4. **Документируйте**: ведите заметки о конфигурации для последующих обновлений
5. **Регулярно обновляйте**: следите за зависимостями и обновлениями Django

### Что дальше?

После успешного деплоя на PythonAnywhere вы можете:

- **Масштабировать**: перейти на платный тариф для увеличения ресурсов
- **Кастомизировать домен**: настроить свой собственный домен
- **Оптимизировать**: настроить кеширование и оптимизацию запросов к БД
- **Мониторить**: настроить систему мониторинга и оповещений
- **Автоматизировать**: настроить CI/CD для автоматического деплоя

---

## Дополнительные темы

### Работа с PostgreSQL (платные тарифы)

#### Создание базы данных

1. Перейдите на вкладку **Databases**
2. Выберите PostgreSQL
3. Укажите имя базы данных
4. Сохраните реквизиты подключения

#### Настройка в Django

```python
# settings.py
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'yourusername$dbname',
        'USER': 'yourusername',
        'PASSWORD': os.environ.get('DB_PASSWORD'),
        'HOST': 'yourusername.postgres.pythonanywhere-services.com',
        'PORT': '10000',
    }
}
```

#### Установка драйвера

```bash
pip install psycopg2-binary
```

### Работа с Celery (фоновые задачи)

Celery позволяет выполнять длительные задачи асинхронно, не блокируя основное приложение.

#### Примеры использования:
- Отправка email-рассылок
- Генерация отчетов
- Обработка загруженных файлов
- Периодические задачи (парсинг, обновление данных)

#### Настройка Celery на PythonAnywhere

**⚠️ Важно:** Celery требует отдельного процесса, который доступен только на платных тарифах.

```python
# myproject/celery.py
import os
from celery import Celery

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'myproject.settings')

app = Celery('myproject')
app.config_from_object('django.conf:settings', namespace='CELERY')
app.autodiscover_tasks()
```

```python
# settings.py
CELERY_BROKER_URL = 'redis://localhost:6379/0'
CELERY_RESULT_BACKEND = 'redis://localhost:6379/0'
CELERY_ACCEPT_CONTENT = ['json']
CELERY_TASK_SERIALIZER = 'json'
```

### Scheduled Tasks (запланированные задачи)

На PythonAnywhere есть встроенная система для запуска задач по расписанию.

#### Настройка

1. Перейдите на вкладку **Tasks**
2. Нажмите **"Create a new scheduled task"**
3. Укажите команду:

```bash
/home/yourusername/.virtualenvs/myenv/bin/python /home/yourusername/project/manage.py custom_command
```

4. Установите время выполнения (например, ежедневно в 3:00)

#### Пример Django management команды

```python
# myapp/management/commands/daily_report.py
from django.core.management.base import BaseCommand
from django.core.mail import send_mail

class Command(BaseCommand):
    help = 'Отправляет ежедневный отчет'

    def handle(self, *args, **kwargs):
        # Ваша логика
        send_mail(
            'Ежедневный отчет',
            'Содержимое отчета',
            'from@example.com',
            ['to@example.com'],
        )
        self.stdout.write(self.style.SUCCESS('Отчет отправлен'))
```

### Настройка Email

#### SMTP настройки

```python
# settings.py
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'  # Или ваш SMTP-сервер
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = os.environ.get('EMAIL_USER')
EMAIL_HOST_PASSWORD = os.environ.get('EMAIL_PASSWORD')
DEFAULT_FROM_EMAIL = 'noreply@yourdomain.com'
```

#### Отправка email

```python
from django.core.mail import send_mail

send_mail(
    'Тема письма',
    'Текст письма',
    'from@example.com',
    ['to@example.com'],
    fail_silently=False,
)
```

### Логирование

#### Настройка логирования

```python
# settings.py
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'handlers': {
        'file': {
            'level': 'ERROR',
            'class': 'logging.FileHandler',
            'filename': '/home/yourusername/project/django_errors.log',
        },
    },
    'loggers': {
        'django': {
            'handlers': ['file'],
            'level': 'ERROR',
            'propagate': True,
        },
    },
}
```

#### Использование логгера

```python
import logging

logger = logging.getLogger(__name__)

def my_view(request):
    try:
        # Ваш код
        pass
    except Exception as e:
        logger.error(f'Ошибка в my_view: {e}')
        raise
```

### Кастомный домен

#### Процесс подключения

1. **Купите домен** у регистратора (Namecheap, GoDaddy, Google Domains)

2. **На платном тарифе PythonAnywhere:**
   - Перейдите на вкладку **Web**
   - Нажмите **"Add a new web app"**
   - Выберите **"Use a custom domain"**
   - Введите ваш домен (example.com)

3. **Настройте DNS у регистратора:**

   Добавьте CNAME записи:
   ```
   Type: CNAME
   Host: www
   Value: yourusername.pythonanywhere.com
   ```

   Для корневого домена (example.com):
   ```
   Type: A
   Host: @
   Value: IP-адрес PythonAnywhere (указан в инструкциях)
   ```

4. **Обновите ALLOWED_HOSTS:**
   ```python
   ALLOWED_HOSTS = [
       'yourusername.pythonanywhere.com',
       'example.com',
       'www.example.com',
   ]
   ```

5. **Подождите DNS propagation** (может занять до 48 часов)

### Резервное копирование

#### Автоматическое резервное копирование БД

```bash
#!/bin/bash
# backup.sh

DATE=$(date +%Y-%m-%d_%H-%M-%S)
BACKUP_DIR="/home/yourusername/backups"
PROJECT_DIR="/home/yourusername/project"

# Создание директории для бэкапов
mkdir -p $BACKUP_DIR

# Копирование базы данных
cp $PROJECT_DIR/db.sqlite3 $BACKUP_DIR/db_$DATE.sqlite3

# Удаление старых бэкапов (старше 7 дней)
find $BACKUP_DIR -name "db_*.sqlite3" -mtime +7 -delete

echo "Backup completed: db_$DATE.sqlite3"
```

Добавьте в **Tasks** для ежедневного выполнения.

### Мониторинг и оптимизация

#### Django Debug Toolbar (только для разработки!)

```python
# settings.py
if DEBUG:
    INSTALLED_APPS += ['debug_toolbar']
    MIDDLEWARE += ['debug_toolbar.middleware.DebugToolbarMiddleware']
    INTERNAL_IPS = ['127.0.0.1']
```

#### Django Silk (для production)

Silk — инструмент для профилирования Django-приложений.

```bash
pip install django-silk
```

```python
# settings.py
INSTALLED_APPS += ['silk']
MIDDLEWARE += ['silk.middleware.SilkyMiddleware']
```

```python
# urls.py
from django.urls import path, include

urlpatterns += [path('silk/', include('silk.urls', namespace='silk'))]
```

#### Оптимизация запросов к БД

```python
# ❌ Плохо - N+1 запросов
posts = Post.objects.all()
for post in posts:
    print(post.author.name)  # Запрос к БД на каждой итерации

# ✅ Хорошо - 1 запрос с JOIN
posts = Post.objects.select_related('author').all()
for post in posts:
    print(post.author.name)  # Данные уже загружены
```

```python
# ❌ Плохо - множество запросов
posts = Post.objects.all()
for post in posts:
    print([tag.name for tag in post.tags.all()])

# ✅ Хорошо - prefetch_related для many-to-many
posts = Post.objects.prefetch_related('tags').all()
for post in posts:
    print([tag.name for tag in post.tags.all()])
```

### Кеширование

#### Redis (требует платный тариф)

```bash
pip install django-redis
```

```python
# settings.py
CACHES = {
    'default': {
        'BACKEND': 'django_redis.cache.RedisCache',
        'LOCATION': 'redis://127.0.0.1:6379/1',
        'OPTIONS': {
            'CLIENT_CLASS': 'django_redis.client.DefaultClient',
        }
    }
}
```

#### Использование кеша

```python
from django.core.cache import cache

# Сохранение в кеш
cache.set('my_key', 'my_value', timeout=3600)  # 1 час

# Получение из кеша
value = cache.get('my_key')

# Удаление
cache.delete('my_key')
```

```python
# Декоратор для кеширования view
from django.views.decorators.cache import cache_page

@cache_page(60 * 15)  # Кеш на 15 минут
def my_view(request):
    return render(request, 'template.html')
```

---

## Best Practices (Лучшие практики)

### 1. Структура проекта

```
myproject/
├── config/                    # Конфигурационная папка
│   ├── settings/
│   │   ├── base.py           # Общие настройки
│   │   ├── development.py    # Для разработки
│   │   └── production.py     # Для продакшена
│   ├── urls.py
│   └── wsgi.py
├── apps/                      # Все приложения проекта
│   ├── users/
│   ├── blog/
│   └── api/
├── static/
├── media/
├── templates/
├── requirements/              # Разделение зависимостей
│   ├── base.txt
│   ├── development.txt
│   └── production.txt
├── .env.example              # Шаблон переменных окружения
├── .gitignore
├── manage.py
└── README.md
```

### 2. Разделение settings

**config/settings/base.py:**
```python
# Общие настройки для всех окружений
from pathlib import Path
import os

BASE_DIR = Path(__file__).resolve().parent.parent.parent

INSTALLED_APPS = [
    'django.contrib.admin',
    # ...
]
```

**config/settings/development.py:**
```python
from .base import *

DEBUG = True
ALLOWED_HOSTS = ['localhost', '127.0.0.1']

# Django Debug Toolbar
INSTALLED_APPS += ['debug_toolbar']
MIDDLEWARE += ['debug_toolbar.middleware.DebugToolbarMiddleware']
```

**config/settings/production.py:**
```python
from .base import *

DEBUG = False
ALLOWED_HOSTS = [os.environ.get('DOMAIN')]

# Безопасность
SECURE_SSL_REDIRECT = True
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
```

### 3. Переменные окружения

**.env.example:**
```
# Django
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Database
DATABASE_URL=postgresql://user:password@localhost/dbname

# Email
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# AWS (если используете S3 для медиа)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_STORAGE_BUCKET_NAME=your-bucket-name
```

### 4. Git workflow

```bash
# Создание .gitignore
echo "*.pyc
__pycache__/
db.sqlite3
/media/
/static/
.env
.DS_Store" > .gitignore

# Первый коммит
git init
git add .
git commit -m "Initial commit"

# Создание ветки для разработки
git checkout -b develop

# Создание feature ветки
git checkout -b feature/new-feature

# После завершения работы
git checkout develop
git merge feature/new-feature
git push origin develop
```

### 5. Тестирование перед деплоем

```bash
# Запуск всех тестов
python manage.py test

# Проверка покрытия тестами
pip install coverage
coverage run --source='.' manage.py test
coverage report

# Проверка безопасности
python manage.py check --deploy
```

### 6. Документирование API

Если вы создаете REST API, используйте автоматическую документацию:

**Django REST Framework + drf-spectacular:**
```bash
pip install djangorestframework drf-spectacular
```

```python
# settings.py
INSTALLED_APPS += [
    'rest_framework',
    'drf_spectacular',
]

REST_FRAMEWORK = {
    'DEFAULT_SCHEMA_CLASS': 'drf_spectacular.openapi.AutoSchema',
}

SPECTACULAR_SETTINGS = {
    'TITLE': 'Your API',
    'DESCRIPTION': 'API description',
    'VERSION': '1.0.0',
}
```

```python
# urls.py
from drf_spectacular.views import SpectacularAPIView, SpectacularSwaggerView

urlpatterns += [
    path('api/schema/', SpectacularAPIView.as_view(), name='schema'),
    path('api/docs/', SpectacularSwaggerView.as_view(url_name='schema'), name='swagger-ui'),
]
```

---

## FAQ (Часто задаваемые вопросы)

### Q: Можно ли использовать PythonAnywhere для коммерческих проектов?
**A:** Да, но на бесплатном тарифе есть ограничения. Для серьезных коммерческих проектов рекомендуется платный тариф от $5/мес.

### Q: Как часто нужно продлевать бесплатный аккаунт?
**A:** Каждые 3 месяца. За неделю до истечения срока придет email-напоминание.

### Q: Можно ли использовать WebSocket на PythonAnywhere?
**A:** На бесплатном тарифе — нет. WebSocket поддерживается на платных тарифах начиная с Hacker ($5/мес).

### Q: Как перенести проект с PythonAnywhere на другой хостинг?
**A:** 
1. Сделайте дамп базы данных
2. Скачайте все файлы проекта
3. Настройте новый сервер аналогичным образом
4. Импортируйте базу данных

### Q: Почему сайт работает медленно?
**A:** На бесплатном тарифе ограничено время CPU (100 сек/день). Если лимит исчерпан, сайт будет работать медленно до следующего дня.

### Q: Можно ли запускать несколько проектов на одном аккаунте?
**A:** Бесплатный тариф позволяет только 1 веб-приложение. Для нескольких проектов нужен платный тариф.

---

## Глоссарий

**CI/CD** — Continuous Integration/Continuous Deployment — автоматизация тестирования и развертывания

**Deployment** — процесс развертывания приложения на сервере

**Environment variables** — переменные окружения для хранения конфиденциальных данных

**Migration** — файлы с изменениями структуры базы данных

**Production** — рабочая среда, где приложение используется реальными пользователями

**Staging** — промежуточная среда для тестирования перед production

**Static files** — файлы CSS, JavaScript, изображения, которые не меняются

**Virtual environment** — изолированное окружение Python с отдельным набором пакетов

**WSGI** — Web Server Gateway Interface — стандарт взаимодействия веб-сервера и Python-приложения

---

## Заключение

Поздравляю! Теперь вы знаете, как развернуть Django-приложение на PythonAnywhere и готовы к публикации своих проектов. 

Помните:
- **Начинайте с простого**: используйте бесплатный тариф для обучения
- **Следите за безопасностью**: никогда не публикуйте секретные ключи
- **Тестируйте локально**: проверяйте изменения перед деплоем
- **Читайте документацию**: Django и PythonAnywhere постоянно обновляются
- **Изучайте логи**: они помогут быстро найти и исправить ошибки

Удачи в ваших проектах! 🚀
--- 
🚀 **ДОПОЛНИТЕЛЬНО**

# WebSocket

**WebSocket — это протокол, который позволяет установить постоянное двустороннее соединение между клиентом и сервером.**

Он работает поверх TCP и позволяет обмениваться данными в режиме **реального времени** без постоянного перезапроса страницы.

---

# 🔌 Как работает WebSocket (простыми словами)

Обычно браузер общается с сервером так:

* отправил запрос → получил ответ → соединение закрыто

Это **HTTP** — он *одноразовый*.

WebSocket делает иначе:

* клиент один раз открывает соединение
* сервер подтверждает
* **соединение остаётся открытым**
* оба (клиент и сервер) могут отправлять сообщения **когда угодно**

---

# ⭐ Что умеет WebSocket

* **двусторонняя связь** (клиент ⇄ сервер)
* **в реальном времени**
* с минимальной задержкой (миллисекунды)
* работает поверх TCP
* передает данные в виде **фреймов**, а не запросов

---

# 🎮 Где используется WebSocket

WebSocket применяют везде, где нужна “живость”:

### ✔ Онлайн-чаты

Сообщения появляются мгновенно.

### ✔ Игры в браузере

Перемещение игроков синхронизируется в реальном времени.

### ✔ Торговые платформы / биржа

Обновление цен без перезагрузки страницы.

### ✔ Социальные сети

Онлайн-статус, уведомления, лайки в реальном времени.

### ✔ Трекеры / карты

Слежение за GPS позицией.

### ✔ Стриминг текстовых данных

Live-обновления, логи.

---

# 🔍 Пример: как выглядит WebSocket-сообщение

Это не HTTP:

```
< client → server >
{"type": "message", "text": "Hello!"}

< server → client >
{"type": "reply", "text": "Hi!"}
```

---

# ⚡ Почему нельзя сделать это на HTTP?

Можно, но:

* нужно каждый раз отправлять запрос
* сервер не может сам присылать данные
* высокая задержка
* лишняя нагрузка

WebSocket решает это благодаря постоянному каналу связи.

---
