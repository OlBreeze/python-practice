
## 1. Магические методы (Dunder methods)

### Что такое и почему важно

* (14:48) Dunder-методы — это специальные методы, начинающиеся и заканчивающиеся двумя подчёркиваниями (например, `__add__`, `__repr__`).
* Они позволяют объектам класса реагировать на встроенные операции: знак +, сравнение, преобразование в строку и др.
* (16:37) Преимущество: мы можем задать свою логику поведения при использовании оператора + между объектами нашего класса, не вызывая метод явно.
* (17:14) Пример: в задаче с дробями мы реализовали класс дробь, и написали свой `__add__`, `__repr__` и др., чтобы складывать дроби и отображать их красиво.

### Как это выглядит в Python

```python
class Fraction:
    def __init__(self, num, denom):
        self.num = num
        self.denom = denom

    def __add__(self, other):
        return Fraction(self.num*other.denom + other.num*self.denom,
                        self.denom*other.denom)

    def __repr__(self):
        return f"{self.num}/{self.denom}"
```

* Тогда: `Fraction(1,2) + Fraction(1,3)` вернёт объект Fraction, и `print(...)` покажет, например, `5/6`.
* Таким образом, мы задаём поведение оператора и красивое отображение.

### Что важно запомнить

* Магические методы облегчают работу с операторами и встроенными функциями.
* Необязательно реализовывать **все** магические методы, только те, которые нужны.
* Важно следовать соглашениям: `__init__`, `__repr__`, `__str__`, `__add__`, `__eq__` и др.
* Избегайте избыточности: если поведение стандартное, можно не переопределять.

---

## 2. Структура класса и объекты в Python

* (17:53) При проектировании классов: сначала описываем **класс** (шаблон), затем создаём **экземпляры** (объекты) этого класса.
* Класс содержит:

  * Поля (атрибуты) — переменные, состояние объекта.
  * Методы — функции, действия, которые объект может выполнять.
* (18:23) Мы можем использовать магические методы или писать собственные методы в зависимости от задачи.
* (19:06) Обсуждение методов `__repr__` и `__str__`:

  * `__repr__` — часто используется для разработчика: способ получить «официальное» представление объекта.
  * `__str__` — для клиента/пользователя: более читаемое строковое представление.

### Пример

```python
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def __repr__(self):
        return f"Person(name={self.name!r}, age={self.age})"

    def __str__(self):
        return f"{self.name}, {self.age} years old"
```

---

## 3. Четыре базовых принципа ООП

* (20:05) В лекции перечислены: инкапсуляція (encapsulation), наследование (inheritance), абстракция (abstraction), полиморфизм (polymorphism).
* Это широко признанные «столпы» ООП. ([Real Python][1])

### 3.1 Инкапсуляция

* (23:03) Инкапсуляция: объединение данных и методов, работающих с ними, внутри класса.
* (24:26) Пример: банковский счёт с приватным балансом — мы не даём прямой доступ к внутреннему атрибуту, а предоставляем методы управления.
* (25:12–29:58) Аналогия: автомобиль — рулевое колесо, педали (публичный интерфейс), внутренности машины спрятаны.
* В Python: соглашения — один подчёркивание `_var` для защищённого, два `__var` — для приватного (name-mangling). ([Programiz][2])

### 3.2 Абстракция

* (50:21) Пример: класс `Figure` или `Animal`, где задаётся общая структура, но не создаётся экземпляр напрямую.
* Абстракция скрывает детали реализации от пользователя и предоставляет простой интерфейс. ([Analytics Vidhya][3])
* Пример:

  ```python
  from abc import ABC, abstractmethod

  class Animal(ABC):
      @abstractmethod
      def move(self):
          pass

  class Dog(Animal):
      def move(self):
          print("Dog runs")
  ```
* Здесь: `Animal` — абстрактный класс; `Dog` реализует конкретное поведение.

### 3.3 Наследование

* (20:56) Наследование: возможность создавать класс-наследник, который получает атрибуты и методы родительского класса.
* Это даёт повторное использование кода и формирует иерархии. ([Python Numerical Methods][4])
* Пример:

  ```python
  class Vehicle:
      def start(self):
          print("Vehicle started")

  class Car(Vehicle):
      def open_trunk(self):
          print("Trunk opened")
  ```
* `Car` наследует `Vehicle`, т.е. имеет метод `start()` и собственный `open_trunk()`.

### 3.4 Полиморфизм

* (20:05) Полиморфизм: один интерфейс — множество реализаций.
* Пример: разные классы реализуют метод с одним и тем же именем, но ведут себя по-разному. ([W3Schools][5])
* Пример:

  ```python
  class Bird:
      def speak(self):
          print("Some generic sound")

  class Dog(Bird):
      def speak(self):
          print("Woof!")

  class Cat(Bird):
      def speak(self):
          print("Meow!")
  ```
* Тогда можно написать:

  ```python
  for animal in [Dog(), Cat()]:
      animal.speak()  # Woof! then Meow!
  ```
* Также: оператор +, `len()` — примеры полиморфизма встроенных функций.

---

## 4. Модификаторы доступа в Python и соглашения

* (32:50–33:54) В других языках: `public`, `protected`, `private`. В Python – соглашения:

  * `_var` — «защищённый» атрибут (неформально).
  * `__var` — «приватный» атрибут, Python делает *name-mangling* (имя изменяется на `_ClassName__var`).
* (35:46) При создании экземпляра видно, что приватные атрибуты не отображаются обычным способом.
* Важно: даже если технически можно получить доступ, лучше **не делать** это вне класса — соблюдение инкапсуляции ведёт к чистому коду.
* Пример из источника: ([Programiz][2])

  ```python
  class Computer:
      def __init__(self):
          self.__max_price = 900

      def sell(self):
          print("Selling Price:", self.__max_price)

      def setMaxPrice(self, price):
          self.__max_price = price
  ```
## 🕵️ Как работает Name Mangling в Python (детальное доп. пояснение)

**Name Mangling** (искажение/преобразование имен) в Python — это механизм, используемый для создания **псевдо-приватности** атрибутов в классах.

### 1\. Что это такое?

Name Mangling автоматически изменяет имена атрибутов, которые начинаются с **двух ведущих символов подчеркивания** (`__`), но **не оканчиваются** ими (т.е., не `__init__`, `__str__` и т.п.).

Его основная цель — предотвратить случайное перезаписывание атрибутов или методов подклассами.

### 2\. Принцип работы

Интерпретатор Python преобразует имя атрибута `__имя_атрибута` внутри тела класса `КлассИмя` в форму `_КлассИмя__имя_атрибута`.

  * **Оригинальное имя:** `__data`
  * **Имя класса:** `MyClass`
  * **Мангл-имя:** `_MyClass__data`

Это преобразование происходит **автоматически** при определении класса и позволяет избежать конфликтов имен (коллизий) между атрибутами базового класса и производного класса.

### 3\. Пример работы Mangling

| Код | Результат в объекте |
| :--- | :--- |
| `self.single_` | `single_` (Не искажается) |
| `self._single` | `_single` (Не искажается, соглашение "защищенный") |
| `self.__mangled` | `_ClassName__mangled` (Искажается) |
| `self.__dunder__` | `__dunder__` (Не искажается, "магические" методы) |

```python
class Parent:
    def __init__(self):
        self.__private_data = "Я приватный"

class Child(Parent):
    def __init__(self):
        super().__init__()
        # Создает свою собственную мангл-переменную: _Child__private_data
        self.__private_data = "Я другой" 

obj = Child()

# Прямой доступ к переменной, созданной в Parent,
# возможен только по мангл-имени:
print(obj._Parent__private_data) 
# Вывод: Я приватный

# Прямой доступ к переменной, созданной в Child:
print(obj._Child__private_data)
# Вывод: Я другой
```

### 4\. Псевдо-приватность

Важно понимать, что name mangling обеспечивает только **псевдо-приватность**. Атрибуты **не являются действительно приватными** (как, например, в Java или C++), поскольку к ним все равно можно получить доступ, используя их преобразованное (мангл-) имя.

Механизм лишь усложняет случайный доступ или перезапись атрибутов извне класса или в подклассах, заставляя разработчика явно указать, что он хочет обойти этот механизм.
---

## 5. Свойства (@property), геттеры и сеттеры, дескрипторы

* (42:45) Рассмотрен механизм: геттеры и сеттеры как методы для доступа/изменения приватных атрибутов.
* (44:26) Использование декоратора `@property` упрощает доступ к свойствам, позволяет обращаться к методам как к атрибутам.
* Пример:

  ```python
  class Person:
      def __init__(self, age):
          self._age = age

      @property
      def age(self):
          return self._age

      @age.setter
      def age(self, value):
          if value < 0:
              raise ValueError("Age cannot be negative")
          self._age = value
  ```
* При таком подходе: `p.age = 30` — работает, но с проверкой.
* (48:17) Далее упоминалось: дескрипторы — более низкоуровневый способ управления атрибутами (реализуя методы `__get__`, `__set__`, `__delete__`).

  * Используется, если необходимо много атрибутов с одинаковой логикой валидации.

---

## 6. Практические примеры и аналогии

* Банкомат (25:12–26:59): демонстрирует инкапсуляцию: пользователь видит интерфейс (клавиатура, дисплей), но не видит внутренности машины (дроссели, контроллеры, связи).
* Автомобиль (27:58–29:27): публичные элементы (руль, педали) vs внутренняя сложная система; аналогично — класс скрывает внутреннюю логику.
* Класс `Animal` / `Figure` (50:21–54:15): пример абстракции — класс без прямых инстансов, только шаблон → потом дочерние классы реализуют/переопределяют методы.

---

## 7. Рекомендации по чистоте кода и интервью

* (39:33) Важно: избегать доступа к защищённым (`_`) или приватным (`__`) атрибутам вне класса, если нет веской причины — это признак качественного кода.
* При подготовке к интервью: знать соглашения доступа, отличия магических методов, свойства `@property`, абстрактные классы и пр.
* Подчёркивается: язык Python допускает динамику, но лучше придерживаться контрактов и соглашений.

---

## 8. Дополнительные моменты, упомянутые в лекции

* (27:32) При карт-переводах, банкоматах и др. множество операций скрыты от пользователя — аналогия к скрытым деталям класса.
* (36:55) При экземпляре класса видно только публичные атрибуты и методы; защищённые/приватные не видны обычным способом.
* (38:34) Константы: в Python нет ключевого слова `const`, поэтому принято писать имена переменных (констант) в ВЕРХНЕМ РЕГИСТРЕ, а переменные — в нижнем регистре.
* (55:14) Было начало обсуждения абстрактного класса `Animal`, про который терминал выдаёт ошибку, если попытаться создать экземпляр без реализации абстрактных методов.

---

## 9. Интеграция с виденным материалом из открытых источников

* Описание принципов ООП (encapsulation, inheritance, abstraction, polymorphism) аналогично описаниям в статьях: “The four key concepts of OOP in Python are…” ([Real Python][1])
* Инкапсуляция: «Encapsulation is the bundling of data (attributes) and methods (functions) within a class, restricting access to some components …» ([GeeksforGeeks][6])
* Полиморфизм: пример с методом `move()` у разных классов — «methods/functions/operators with the same name that can be executed on many objects or classes» ([W3Schools][5])
* Наследование: «Inheritance allows us to define a class that inherits all the methods and attributes from another class …» ([Python Numerical Methods][4])

---

## 10. Что рекомендовано сделать после лекции

* Переписать дома простой класс, включив:

  * Магический метод `__add__` (или другой)
  * Публичные, защищённые и приватные атрибуты, используя `_` и `__`
  * Свойство `@property` и сеттер для проверки значения
  * Абстрактный базовый класс и два-три наследника с переопределением метода
* Подумать: где в вашем текущем проекте (например, на Python) можно применить магические методы или дескрипторы.
* На интервью: будьте готовы пояснить, зачем нужны dunder-методы, как работает name-mangling в Python, что такое абстрактный класс и когда его использовать.

---

Если хочешь — могу подготовить **слайды** или **пример кода** по каждому разделу или даже **качественную шпаргалку-чеклист** по ООП в Python (Уровень B2/Intermediate). Хочешь?

[1]: https://realpython.com/python3-object-oriented-programming/?utm_source=chatgpt.com "Object-Oriented Programming (OOP) in Python"
[2]: https://www.programiz.com/python-programming/object-oriented-programming?utm_source=chatgpt.com "Python Object Oriented Programming (With Examples) - Programiz"
[3]: https://www.analyticsvidhya.com/blog/2020/09/object-oriented-programming/?utm_source=chatgpt.com "Basic Object Oriented Programming (OOPs) Concepts in Python"
[4]: https://pythonnumericalmethods.studentorg.berkeley.edu/notebooks/chapter07.03-Inheritance-Encapsulation-and-Polymorphism.html?utm_source=chatgpt.com "Inheritance, Encapsulation and Polymorphism"
[5]: https://www.w3schools.com/python/python_polymorphism.asp?utm_source=chatgpt.com "Python Polymorphism - W3Schools"
[6]: https://www.geeksforgeeks.org/python/python-oops-concepts/?utm_source=chatgpt.com "Python OOPs Concepts - GeeksforGeeks"
