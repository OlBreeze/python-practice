# Домашнее задание: Django Ninja API Projects

## 📋 Общие требования

Для **каждого проекта** необходимо реализовать:

- ✅ **CRUD операции** для основных сущностей
- 🔐 **Аутентификация пользователей** (все операции только для зарегистрированных пользователей)
- 📚 **Автоматическая документация Swagger UI**
- 🧪 **Тестирование через Postman**
- ✔️ **Валидация данных через Pydantic схемы**
- 🎯 **Использование декораторов для защиты endpoints**

---

## Проект 1: API для управления задачами (Task Manager)

### Описание
Разработать API для управления задачами, где пользователи могут создавать, обновлять, удалять и просматривать задачи.

### Основные функции

#### Модели
- **Task** (Задача):
  - `title` — название задачи
  - `description` — описание
  - `status` — статус (выполнено/не выполнено)
  - `deadline` — срок выполнения
  - `created_at` — дата создания
  - `user` — владелец задачи (ForeignKey)

#### Требуемый функционал
1. **CRUD операции** для задач
2. **Фильтрация** задач по статусу ("выполнено", "не выполнено")
3. **Сортировка** задач по дате создания или сроку выполнения
4. **Аутентификация** — только авторизованные пользователи могут управлять своими задачами
5. Пользователь видит только **свои задачи**

#### Endpoints
```
POST   /api/tasks/          - Создание задачи
GET    /api/tasks/          - Список всех задач пользователя (с фильтрацией)
GET    /api/tasks/{id}/     - Детали конкретной задачи
PUT    /api/tasks/{id}/     - Обновление задачи
DELETE /api/tasks/{id}/     - Удаление задачи
```

#### Визуализация
- Swagger UI для документации
- Коллекция Postman для тестирования

---

## Проект 2: API для онлайн-магазина (E-commerce)

### Описание
Разработать API для управления продуктами и заказами в интернет-магазине.

### Основные функции

#### Модели
- **Product** (Товар):
  - `name` — название
  - `description` — описание
  - `price` — цена
  - `stock` — количество на складе
  - `category` — категория

- **Cart** (Корзина):
  - `user` — пользователь
  - `product` — товар
  - `quantity` — количество

- **Order** (Заказ):
  - `user` — пользователь
  - `status` — статус (в процессе, отправлен, доставлен)
  - `total_price` — общая стоимость
  - `created_at` — дата создания

#### Требуемый функционал
1. **CRUD операции** для товаров
2. **Операции с корзиной**:
   - Добавление товаров в корзину
   - Удаление товаров из корзины
   - Просмотр корзины
3. **Оформление заказа** из корзины
4. **Управление статусом заказов**
5. **Аутентификация** — только авторизованные пользователи

#### Endpoints
```
# Товары
GET    /api/products/          - Список товаров
POST   /api/products/          - Создание товара (только админ)
GET    /api/products/{id}/     - Детали товара
PUT    /api/products/{id}/     - Обновление товара
DELETE /api/products/{id}/     - Удаление товара

# Корзина
GET    /api/cart/              - Просмотр корзины
POST   /api/cart/add/          - Добавление в корзину
DELETE /api/cart/remove/{id}/  - Удаление из корзины

# Заказы
POST   /api/orders/            - Оформление заказа
GET    /api/orders/            - Список заказов пользователя
GET    /api/orders/{id}/       - Детали заказа
PATCH  /api/orders/{id}/status/ - Изменение статуса (только админ)
```

---

## Проект 3: API для управления коллекцией фильмов (Movie Collection)

### Описание
Создать API для управления коллекцией фильмов, включая операции с фильмами, жанрами, рейтингами и отзывами.

### Основные функции

#### Модели
- **Movie** (Фильм):
  - `title` — название
  - `description` — описание
  - `release_date` — дата выхода
  - `rating` — рейтинг (0-10)
  - `genres` — жанры (ManyToMany)

- **Genre** (Жанр):
  - `name` — название жанра

- **Review** (Отзыв):
  - `movie` — фильм
  - `user` — пользователь
  - `rating` — оценка (1-10)
  - `comment` — комментарий
  - `created_at` — дата создания

#### Требуемый функционал
1. **CRUD операции** для фильмов
2. **Добавление жанров** и **отзывов**
3. **Фильтрация** фильмов по жанрам, рейтингу и дате выпуска
4. **Поиск** фильмов по частичному совпадению названия
5. **Аутентификация** для добавления отзывов

#### Endpoints
```
# Фильмы
GET    /api/movies/           - Список фильмов (с фильтрацией и поиском)
POST   /api/movies/           - Создание фильма
GET    /api/movies/{id}/      - Детали фильма
PUT    /api/movies/{id}/      - Обновление фильма
DELETE /api/movies/{id}/      - Удаление фильма

# Жанры
GET    /api/genres/           - Список жанров
POST   /api/genres/           - Создание жанра

# Отзывы
POST   /api/movies/{id}/reviews/  - Добавление отзыва
GET    /api/movies/{id}/reviews/  - Список отзывов к фильму
```

---

## Проект 4: API для блога (Blog Platform)

### Описание
Разработать REST API для управления постами в блоге.

### Основные функции

#### Модели
- **Post** (Пост):
  - `title` — заголовок
  - `content` — содержание
  - `author` — автор (ForeignKey)
  - `tags` — теги (ManyToMany)
  - `created_at` — дата создания
  - `updated_at` — дата обновления

- **Tag** (Тег):
  - `name` — название тега

- **Comment** (Комментарий):
  - `post` — пост
  - `author` — автор
  - `content` — содержание
  - `created_at` — дата создания

#### Требуемый функционал
1. **CRUD операции** для постов
2. **Добавление комментариев** к постам
3. **Управление тегами** для каждого поста
4. **Аутентификация** — только авторизованные пользователи могут создавать/редактировать посты
5. Автор может редактировать только **свои посты**

#### Endpoints
```
# Посты
GET    /api/posts/            - Список постов
POST   /api/posts/            - Создание поста
GET    /api/posts/{id}/       - Детали поста
PUT    /api/posts/{id}/       - Обновление поста (только автор)
DELETE /api/posts/{id}/       - Удаление поста (только автор)

# Комментарии
POST   /api/posts/{id}/comments/  - Добавление комментария
GET    /api/posts/{id}/comments/  - Список комментариев

# Теги
GET    /api/tags/             - Список тегов
POST   /api/tags/             - Создание тега
```

---

## Проект 5: API для мониторинга состояния серверов (Server Monitoring)

### Описание
Создать API для мониторинга состояния серверов и их метрик.

### Основные функции

#### Модели
- **Server** (Сервер):
  - `name` — название
  - `ip_address` — IP адрес
  - `status` — статус (включен/выключен)
  - `owner` — владелец (ForeignKey)

- **Metric** (Метрика):
  - `server` — сервер
  - `cpu_usage` — загрузка процессора (%)
  - `memory_usage` — использование памяти (%)
  - `disk_usage` — использование диска (%)
  - `timestamp` — время измерения

- **Alert** (Оповещение):
  - `server` — сервер
  - `message` — сообщение
  - `severity` — уровень (warning, critical)
  - `created_at` — время создания

#### Требуемый функционал
1. **CRUD операции** для серверов
2. **Отслеживание метрик** серверов
3. **Отправка оповещений** при достижении критических значений метрик
4. **Аутентификация** — пользователи видят только свои серверы

#### Endpoints
```
# Серверы
GET    /api/servers/          - Список серверов
POST   /api/servers/          - Добавление сервера
GET    /api/servers/{id}/     - Детали сервера
PUT    /api/servers/{id}/     - Обновление сервера
DELETE /api/servers/{id}/     - Удаление сервера

# Метрики
POST   /api/servers/{id}/metrics/  - Добавление метрики
GET    /api/servers/{id}/metrics/  - История метрик

# Оповещения
GET    /api/alerts/           - Список оповещений
```

---

## Проект 6: API для управления книгами (Book Library)

### Описание
Разработать API для управления библиотекой книг.

### Основные функции

#### Модели
- **Book** (Книга):
  - `title` — название
  - `author` — автор
  - `genre` — жанр
  - `isbn` — ISBN код
  - `available` — доступна ли книга

- **Rental** (Аренда):
  - `book` — книга
  - `user` — пользователь
  - `rental_date` — дата аренды
  - `return_date` — дата возврата (null если не возвращена)

#### Требуемый функционал
1. **CRUD операции** для книг
2. **Поиск книг** по названию, автору или жанру
3. **Аренда книг** пользователями
4. **Возврат книг**
5. **Аутентификация** — только зарегистрированные пользователи

#### Endpoints
```
# Книги
GET    /api/books/            - Список книг (с поиском)
POST   /api/books/            - Добавление книги
GET    /api/books/{id}/       - Детали книги
PUT    /api/books/{id}/       - Обновление книги
DELETE /api/books/{id}/       - Удаление книги

# Аренда
POST   /api/books/{id}/rent/  - Взять книгу
POST   /api/books/{id}/return/ - Вернуть книгу
GET    /api/rentals/          - Мои арендованные книги
```

---

## Проект 7: API для управления студентами и курсами (Student Course Management)

### Описание
Создать API для управления студентами, курсами и результатами экзаменов.

### Основные функции

#### Модели
- **Student** (Студент):
  - `first_name` — имя
  - `last_name` — фамилия
  - `email` — email
  - `enrollment_date` — дата зачисления

- **Course** (Курс):
  - `name` — название курса
  - `description` — описание
  - `instructor` — преподаватель
  - `credits` — количество кредитов

- **Enrollment** (Регистрация на курс):
  - `student` — студент
  - `course` — курс
  - `grade` — оценка (0-100)
  - `enrollment_date` — дата регистрации

#### Требуемый функционал
1. **CRUD операции** для студентов и курсов
2. **Регистрация студентов** на курсы
3. **Выставление оценок** студентам
4. **Подсчет средней оценки** по каждому курсу
5. **Аутентификация** — преподаватели и студенты с разными правами

#### Endpoints
```
# Студенты
GET    /api/students/         - Список студентов
POST   /api/students/         - Добавление студента
GET    /api/students/{id}/    - Детали студента
PUT    /api/students/{id}/    - Обновление студента
DELETE /api/students/{id}/    - Удаление студента

# Курсы
GET    /api/courses/          - Список курсов
POST   /api/courses/          - Создание курса
GET    /api/courses/{id}/     - Детали курса
PUT    /api/courses/{id}/     - Обновление курса

# Регистрация
POST   /api/enrollments/      - Регистрация на курс
GET    /api/courses/{id}/students/ - Студенты курса
PATCH  /api/enrollments/{id}/grade/ - Выставление оценки
GET    /api/courses/{id}/average/   - Средняя оценка по курсу
```

---

## 🔐 Требования по аутентификации

Для всех проектов необходимо реализовать:

### 1. Базовая аутентификация через токен

```python
from ninja.security import HttpBearer
from django.contrib.auth.models import User

class AuthBearer(HttpBearer):
    def authenticate(self, request, token):
        try:
            # Здесь можно использовать Django Token Authentication
            # или JWT токены
            user = User.objects.get(auth_token=token)
            return user
        except User.DoesNotExist:
            return None
```

### 2. Защита endpoints декораторами

```python
from ninja import Router

router = Router()

@router.post("/", auth=AuthBearer())
def create_item(request, data: ItemIn):
    # Только авторизованные пользователи
    pass
```

### 3. Регистрация и вход

Каждый проект должен иметь endpoints:
```
POST /api/auth/register/  - Регистрация
POST /api/auth/login/     - Вход (получение токена)
POST /api/auth/logout/    - Выход
GET  /api/auth/me/        - Информация о текущем пользователе
```

---

## 📦 Структура проекта

```
project_name/
├── manage.py
├── requirements.txt
├── project_name/
│   ├── __init__.py
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
└── app_name/
    ├── __init__.py
    ├── models.py
    ├── schemas.py
    ├── api.py
    ├── auth.py
    └── admin.py
```

---


## 📚 Полезные ресурсы

- [Django Ninja Documentation](https://django-ninja.rest-framework.com/)
- [Pydantic Documentation](https://docs.pydantic.dev/)
- [Postman Learning Center](https://learning.postman.com/)
- [Swagger/OpenAPI Guide](https://swagger.io/docs/)

---

## ✅ Чек-лист перед сдачей

- [ ] Все CRUD операции работают
- [ ] Аутентификация реализована
- [ ] Swagger UI доступна по `/api/docs`
- [ ] Коллекция Postman экспортирована
- [ ] Валидация данных настроена
- [ ] README.md с инструкциями по запуску
- [ ] requirements.txt с зависимостями
- [ ] Код отформатирован и прокомментирован

---
