
## 🧱 3. Структура Django-застосунку

**Стандартні файли:**
```

**init**.py
admin.py
apps.py
models.py
tests.py
views.py
urls.py

````

**Можливі додаткові:**
- `forms.py` — для форм;
- `serializers.py` — для DRF;
- `managers.py` — кастомні менеджери;
- `permissions.py` — логіка доступу;
- `utils.py` — допоміжні функції.

---

## 🧪 4. Тестування у Django

### 4.1. Мета тестування
- Перевірка якості та відповідності вимогам.
- Зниження кількості помилок у продакшені.
- Прискорення рефакторингу та стабільність коду.

### 4.2. Типи тестування
| Тип | Опис |
|------|------|
| **Unit testing** | Перевірка окремих функцій/методів |
| **Integration testing** | Взаємодія між модулями |
| **System testing** | Повна перевірка додатку |
| **Manual / Automated** | Ручне або автоматизоване тестування |

---

## 🔍 5. Валідація vs Верифікація

| Термін | Пояснення | Питання, на яке відповідає |
|---------|------------|-----------------------------|
| **Верифікація** | Відповідність продукту специфікаціям | “Чи правильно ми будуємо продукт?” |
| **Валідація** | Відповідність потребам користувача | “Чи той продукт ми будуємо, що потрібно?” |

---

## ⚡ 6. Test-Driven Development (TDD)

1. Спочатку пишемо **тест**.  
2. Потім реалізуємо **код**, щоб тест пройшов.  
3. Далі — **рефакторинг**.

Переваги:
- менше помилок;
- швидше виявлення дефектів;
- легше підтримувати код.

---

## 🧰 7. Інструменти для тестування

| Модуль | Характеристика |
|---------|----------------|
| `unittest` | Вбудований у Python, ООП-підхід (класи `TestCase`) |
| `pytest` | Потрібна установка, підтримує функціональний стиль тестів |

**Приклади тверджень (`assert`):**
- `assertEqual(a, b)`
- `assertNotEqual(a, b)`
- `assertTrue(x)`
- `assertFalse(x)`
- `assertIn(x, y)`

---

## 🧩 8. Приклад юніт-тесту у Django

```python
from django.test import TestCase
from .models import Product

class ProductModelTest(TestCase):
    def setUp(self):
        self.product = Product.objects.create(
            name='Django',
            price=100,
            quantity=10
        )

    def test_product_creation(self):
        self.assertEqual(self.product.name, 'Django')
        self.assertEqual(self.product.price, 100)
        self.assertEqual(self.product.quantity, 10)
````

---

## 📈 9. Висновки

* Django має вбудований захист від більшості типових атак.
* Безпека забезпечується через **middleware**, **налаштування** і **використання ORM**.
* **Юніт-тестування** — основа для перевірки функціональності додатка.
* Використання **unittest/pytest** допомагає швидше виявляти помилки.
* TDD підхід підвищує якість і надійність коду.

---
