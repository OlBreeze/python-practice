
Лекція: **UNIT_01_UA (26.08.2025) Python Pro (29.08.2025)**
[https://www.youtube.com/watch?v=DJ28oQ_lj4g](https://www.youtube.com/watch?v=DJ28oQ_lj4g)

---

## 💡 Стиль коду та конвенції (РЕР 8)

* **Відступи (Специфікація РЕР 8):**
    * Між **класами** та **функціями** (визначеннями) залишати **два порожні рядки**.
    * Останній рядок файлу повинен бути **порожнім** (один порожній рядок).
* **Документація:**
    * **Docstrings (рядки документації)** є обов'язковими (must-have). Рекомендовано ознайомитися з **РЕР 250** щодо їх написання.
* **Іменування об'єктів:**  

    | Об'єкт     | Стиль іменування     | Приклад    |  
    | :--- | :--- | :--- |  
    | **Змінні/Функції** | **Нижній регістр** та **нижнє підкреслення** для декількох слів (snake_case) | `my_variable`, `calculate_sum` |  
    | **Константи** | **Верхній регістр** та **нижнє підкреслення** (SCREAMING_SNAKE_CASE) | `MAX_VALUE` |  
    | **Класи** | **Велика літера** для кожного слова (CapWords / PascalCase), **без підкреслень** | `MyClass`, `LoggerFactory` |  
    | **Модулі/Файли** | **Нижній регістр** та **нижнє підкреслення** | `my_module.py` |  
* **Інструменти:** Для автоматизованої перевірки якості коду на відповідність РЕР 8 використовуйте **лінтери** (наприклад, `pylint`).  
У PyCharm для автовирівнювання можна використовувати комбінацію клавіш `Ctrl + Shift + Alt + L`.

---

## 🛠️ Функції

* **Визначення:** Функція – це **алгоритм** (іменований блок коду), який виконує **певну процедуру/завдання**.
    * **Метод** – це та сама функція, але визначена **в межах класу**.
* **Параметри та результат:**
    * Може приймати **будь-яку кількість вхідних параметрів**.
    * Завжди **повертає тільки один об'єкт** (`return`). Якщо потрібно повернути декілька значень, вони повертаються як один об'єкт-котреж (tuple).
* **Життєвий цикл (Стек):**
    * Після виконання функції та повернення результату (`return`) пам'ять, виділена для локальних змінних, **звільняється** (працює автоматичний garbage-колектор). Це принцип **LIFO** (Last In, First Out).

---

## 🌐 Області видимості (LEGB)

* **LEGB** – це правило визначення порядку пошуку імен об'єктів: **L**ocal (Локальний), **E**nclosed (Вкладений), **G**lobal (Глобальний), **B**uilt-in (Вбудований).
* **Локальна область видимості (L):**
    * Працює **в межах функції** (всередині алгоритму).
    * **Перевага:** Ефективне використання пам'яті (пам'ять звільняється після `return`).
    * **Недолік:** Результат або стан **не зберігається** і не може бути повторно використаний без повторного виклику функції (якщо результат не зберегти у глобальній змінній).
* **Глобальна область видимості (G):**
    * Визначена **поза межами функцій** (на рівні модуля).
    * **Перевага:** Стан/значення може **повторно використовуватися** (shared) у різних частинах коду, наприклад, між файлами.
    * **Недолік:** Об'єкт **постійно займає пам'ять**. Зміна його стану в одній точці коду призводить до **зміни для всіх подальших посилань** (небезпека колізій).
* **Доступ до змінних:**
    * З локальної області видимості **можна отримати доступ (читати)** до об'єктів із вищих рівнів (Global, Built-in).
    * **Не можна** просто так **перевизначити (змінити)** об'єкт із вищого рівня. При спробі присвоєння значення створюється **нова локальна змінна** з тим самим іменем, яка **затінює** глобальну (shadowing). Для зміни глобальної змінної необхідно використовувати ключове слово `global`.
---

## 📝 Конспект лекции: Функции и Области видимости (LEGB) в Python

### II. 🛠️ Функции

* **Определение функции:**
    * Функция — это **именованный алгоритм** (именованный блок кода), который выполняет определенную процедуру или задачу.
    * **Метод** — это функция, определенная **внутри класса**.
* **Синтаксис:** Определяется с помощью ключевого слова `def`, за которым следует имя, скобки для аргументов и двоеточие (`def function_name(args):`).
    * Базовые функции и классы определяются **от начала строки** (без отступа).
* **Вложенность (Инкапсуляция):**
    * Внутри функции может быть определена **вложенная функция** (`inner function`). Это создает отдельную локальную область видимости (Enclosed).
    * **Вложенные функции недоступны** для вызова за пределами внешней функции.
* **Тип данных (Типизация):**
    * **Тип данных** — это **множество значений** и **множество операций** (атрибутов), применимых к этим значениям.

---

### III. 🌐 Области видимости (LEGB)

Python использует четыре области видимости для поиска имен объектов: **L**ocal, **E**nclosed, **G**lobal, **B**uilt-in.

| Область | Описание | Принцип работы |
| :--- | :--- | :--- |
| **Local (L)** | Внутри **текущей функции** или метода. | **Преимущество:** Экономия памяти (память очищается после `return`). **Недостаток:** Результат не сохраняется для повторного использования без сохранения его в переменной. |
| **Enclosed (E)** | Внутри **окружающей (внешней)** функции, содержащей вложенную. | Используется во вложенных функциях. Для изменения переменной требуется **`nonlocal`** (дополнение). |
| **Global (G)** | На уровне **модуля** (вне функций/классов). | **Преимущество:** Доступность и возможность "шаринга" состояния между функциями/модулями. **Недостаток:** Занимает память **постоянно**. Перезапись влияет на все места в коде. |
| **Built-in (B)** | Встроенные функции и константы (напр., `print`, `len`, `int`, `str`). | Всегда доступны без импорта. |

#### Правила доступа и изменения

1.  **Доступ (Чтение):** Из низшего уровня (L) можно **получить доступ** к именам на всех высших уровнях (E, G, B).
2.  **Изменение (Перезапись):**
    * Без использования ключевых слов (`global`, `nonlocal`), попытка присвоить значение переменной из внешней (E или G) области видимости в локальной (L) области приводит к созданию **новой локальной переменной** с тем же именем. Это называется **затенением (shadowing)**.
    * В результате, внешняя (глобальная) переменная **не изменяется**.

#### ⚠️ Рекомендации по именованию

* **Избегайте совпадения имен** (`ідентифікаторів`) объектов на разных уровнях видимости (L, E, G), чтобы избежать конфликтов и путаницы (shadowing).
* Избегайте использования **однобуквенных имен** (кроме общепринятых счетчиков, как в цикле `for i...`), чтобы код был максимально понятен.
* Для конфликтующих импортированных объектов (напр., две функции `time` из разных модулей) используйте **псевдонимы** (`as`): `import time as t1`.
Конечно. Продолжаю конспект лекции с того места, где остановились.

-----

### IV. 🔄 Функции: Взаимодействие с областями видимости

  * **Вложенные функции (Inner/Nested Functions):**
      * Создание функции внутри другой функции (`outer function`) является распространенным приемом, особенно при работе с паттерном **Декоратор**.
      * Вложенная функция создает **свою собственную локальную область видимости (L)**, которая находится в **Enclosed (E)** области видимости внешней функции.
      * **Недоступность:** Вложенная функция **не может быть вызвана** извне внешней функции.
  * **Правило LEGB при поиске:**
      * Поиск имени объекта всегда идет последовательно: **L**ocal $\rightarrow$ **E**nclosed $\rightarrow$ **G**lobal $\rightarrow$ **B**uilt-in.
      * Поиск прекращается при первом нахождении имени.
  * **Попытка изменения переменной во вложенной функции:**
      * Если во вложенной функции попытаться **переопределить** (присвоить новое значение) переменной, которая существует на уровне внешней функции (Enclosed), то по умолчанию будет создана **новая локальная переменная** внутри вложенной функции.
      * В результате, переменная во внешней (Enclosed) области **не изменится**. (Для изменения требуется ключевое слово `nonlocal`).
  * **Общие правила именования (Повторение и дополнение):**
      * **Нежелательно** создавать переменные, функции или классы с **именами (идентификаторами), которые уже существуют** на более высоких уровнях (Global или Built-in), чтобы избежать коллизий и путаницы (эффекта "затенения" - shadowing).
      * При импорте объектов (функций, методов) с одинаковыми именами из разных модулей используйте **псевдоним** (`import module as alias`) для избежания конфликтов:
        ```python
        # Пример: time из модуля time и time из асинхронного модуля
        import time 
        import asyncio.time as async_time # Используем псевдоним 
        ```
        .
      * В циклах (`for`) итерационные переменные (например, `i`) следует заменять **конкретными, релевантными именами** (`for element in list:`) для лучшей читаемости кода, особенно в больших проектах.

-----

### V. 🔄 Функции: Параметры и возвращаемое значение (Обзор)

  * **Количество аргументов:** Функция может принимать **сколько угодно** (скільки завгодно) вхідних параметрів.
  * **Возвращаемое значение (`return`):** Функция всегда **возвращает только один объект**.
      * Если нужно вернуть несколько значений, они автоматически упаковываются в один объект — **кортеж (tuple)**.
  * **Использование результата:**
      * Если результат нужен **однократно**, его можно сразу использовать (например, распечатать): `print(my_function(4, 10))`.
      * Если результат нужен **повторно** или его состояние нужно сохранить, его следует **сохранить в переменной**: `result = my_function(4, 10)`.

-----

### VI. 💾 Управление памятью

  * **Стек (Stack):** Работа функции с локальными переменными осуществляется по принципу **LIFO** (Last-In, First-Out, "останній прийшов, перший пішов").
  * **Очистка памяти (Garbage Collector):**
      * Как только функция завершает работу и возвращает результат (`return`), память, выделенная для **локальных объектов**, **автоматически освобождается** (звільняється).
      * В Python работает **автоматический сборщик мусора (garbage collector)**, который отслеживает, сколько ссылок (посилань) существует на объект. Если ссылок нет, память освобождается.
Продолжаю конспект лекции.

---

### VII. 🧩 Прочие важные концепции

* **Пространство имен (Namespace):**
    * **Определение:** Это **именованная среда**, представляющая собой **совокупность объектов**, с которыми можно работать.
    * **Функция:** Позволяет применять эти объекты (в Python все является объектом) при построении решений.
* **Область видимости (Scope):**
    * **Определение:** Это определенная область, в пределах которой доступны **имена** (функций, переменных, констант, объектов).
* **Итератор и Паттерны Проектирования:**
    * **Итератор** – это один из **паттернов проектирования**, который дает возможность организовать **перебор элементов** (итерирование) в любых итерируемых объектах (списки, кортежи, строки, файлы).
    * **Цель:** Организовать перебор, **скрывая детали реализации** этого перебора.
* **Сложные структуры данных:**
    * При построении сложных структур (например, особого списка), можно использовать **вложенность классов**, где одна часть класса отвечает за структуру элемента, а другая — за его позицию и служебные атрибуты (для итерации).

---

### VIII. ⚠️ Вопросы для собеседований

Основные вопросы, которые часто задают по теме функций:

1.  **Что такое функция?** (Именованный алгоритм/блок кода для выполнения определенной процедуры).
2.  **Сколько параметров** может принимать функция на вход? (Сколько угодно).
3.  **Сколько объектов** может вернуть функция? (Только **один** — если нужно несколько, то возвращается кортеж).
4.  **Правило LEGB** (Local, Enclosed, Global, Built-in).
    * **Можем ли мы** из локальной области видимости **получать доступ** (читать) к объектам с более высоких уровней (E, G, B)? (**Так, можемо** — да, можем).
    * **Можем ли мы** из локальной области видимости **переопределить** (изменить) глобальный объект без ключевых слов? (**Ні, не можемо**) — будет создана новая локальная переменная (shadowing).

---

### IX. 🗂️ Примеры и нюансы использования

| Сценарий | Преимущество | Недостаток/Ограничение |
| :--- | :--- | :--- |
| **Локальная переменная** (внутри функции) | **Эффективное использование памяти**, т.к. память освобождается после `return` (с помощью GC). | **Нельзя повторно использовать** результат без повторного вызова функции или сохранения его в глобальной переменной. |
| **Глобальная переменная** (за пределами функций) | Состояние **сохраняется** и может использоваться **повторно** в разных частях кода или модулях (например, остатки на складе). | **Постоянно занимает память**. Изменение состояния в одном месте кода **перезаписывает** ее для всех последующих вызовов, что может привести к некорректным результатам (коллизиям). |
| **Несоответствие имен** (напр., `x` в функции и `x` на глобальном уровне) | **Не рекомендовано**, т.к. ведет к путанице и коллизиям. IDE (PyCharm) часто подсвечивает это желтой волнистой линией. | |

## 📚 Дополнение: Функции, Аргументы и LEGB

Продолжаю конспект лекции, добавляя ключевые моменты из оставшейся части транскрипта.

---

### X. 🔢 Аргументы функций: Особенности

* **Опциональные (необязательные) параметры:**
    * Якщо при визначенні функції для параметра **не встановлено значення за замовчуванням** (наприклад, `def func(x, y):`), то при виклику функції значення для цього параметра є **обов'язковим**.
    * При наявності опціональних параметрів, при використанні **оператора присвоєння** (`=`) у формальному визначенні функції (`def func(x=10, y=20):`), **не встановлюються пробіли** між ім'ям, оператором та значенням. Це виняткова ситуація, що відрізняється від присвоєння змінних у тілі коду.
* **Рекомендації щодо іменування (Повторення):**
    * Щоб уникнути **колізій** та **плутанини**, рекомендовано, щоб імена об'єктів при **формальному визначенні** функції (параметри) та імена об'єктів при **фактичному виклику** функції (аргументи) **не співпадали** з ідентифікаторами глобальних змінних.

---

### XI. ⚙️ Управління глобальними змінними (Global)

* **Переваги глобальних змінних:** Вони корисні, коли необхідно **проводити розрахунок** багато разів, використовуючи **один і той самий стан** (наприклад, залишки на складі), і при цьому не потрібно підтверджувати зміну цього стану (тобто змінна лише читається).
* **Ризик зміни стану:** Якщо глобальна змінна **перезаписується** на будь-якому етапі виконання коду, всі **подальші посилання** на цей об'єкт будуть використовувати **нове, змінене значення**.
    * *Приклад:* При одночасному замовленні, зміна глобальної змінної "залишки" одним користувачем призводить до **некоректних розрахунків** для інших, які посилаються на старе значення.

---

### XII. 🚪 Області видимості: Читання vs. Запис

* **Доступ (Читання):**
    * Функція (локальний рівень) **може** отримати доступ до глобальної змінної для читання її значення. Це корисно, наприклад, для налаштування діапазону в циклі на основі глобальної константи.
* **Перевизначення (Запис):**
    * Без використання ключового слова `global`, спроба присвоїти значення глобальній змінній всередині функції призводить до створення **нової локальної змінної** з тим самим іменем (shadowing). Глобальна змінна **залишається незмінною**.
    * Це створює **конфліктну ситуацію**, яку IDE підкреслює як потенційну проблему. Рекомендовано уникати однакових ідентифікаторів на різних рівнях.

---

### XIII. 🧱 Вкладеність функцій (Enclosed/Local)

* **Вкладена функція (`inner function`):** Є **окремою локальною областю видимості** для свого тіла.
* **Доступ до зовнішньої функції (`outer function`):**
    * З вкладеної функції **можна читати** змінні зовнішньої функції (як доступ до вищого рівня E).
    * Спроба **перевизначити** змінну зовнішньої функції без ключового слова `nonlocal` призводить до створення **локальної змінної** всередині вкладеної функції.

## 🔑 Важливі доповнення до теми "Функції та Області видимості (LEGB)"

Згідно з Вашим запитом, я додам ключові моменти, які розширюють розуміння функцій та правила LEGB, особливо щодо зміни значень.

-----

### 1\. 📢 Ключове слово `nonlocal` (Область видимості Enclosed)

Лектор згадував про 4 області видимості (Local, Enclosed, Global, Built-in - **LEGB**), але детально зупинився лише на Local та Global. Ключове слово `nonlocal` є критично важливим для розуміння **Enclosed (E)** рівня:

  * **Призначення:** Дозволяє **змінювати** (переприсвоювати) значення змінної, яка знаходиться у **найближчій зовнішній (охоплюючій) функції**, але **не є глобальною**.
  * **Використання:** Застосовується виключно у **вкладених функціях** (функція всередині функції).
  * **Чому це важливо:**
      * За замовчуванням, як і для глобальних змінних, спроба присвоїти значення змінній з охоплюючої функції призведе до створення **нової локальної змінної** всередині внутрішньої функції (shadowing).
      * Ключове слово `nonlocal` запобігає цьому, вказуючи інтерпретатору, що потрібно змінити змінну в батьківській області видимості.

 **Приклад:**

 ```python
 def outer():
     count = 0  # Змінна в Enclosing (E) області
     def inner():
         nonlocal count # Без 'nonlocal' тут створилася б нова локальна змінна 'count'
         count += 1
         print(f"Inner count: {count}")
     inner()
     print(f"Outer count: {count}")
 ```

```
outer()

Output:

Inner count: 1

Outer count: 1 (Значення в зовнішній функції ЗМІНИЛОСЯ)
```
-----

### 2. ⚠️ Проблема `UnboundLocalError` (Особливість Local Scope)

Це поширена помилка, пов'язана з тим, як Python визначає локальність змінної **під час компіляції** (на відміну від часу виконання):

  * **Правило:** Якщо ви **присвоюєте** значення імені де-небудь у тілі функції (наприклад, `x = 10`), Python автоматично розглядає це ім'я як **локальне** для цієї функції, незалежно від того, чи існує таке саме ім'я в глобальній області.
  * **Помилка:** Якщо ви спробуєте **прочитати** (використати) цю змінну *до* того, як їй буде присвоєно значення всередині функції, а вона вже була позначена як локальна через подальше присвоєння, ви отримаєте помилку `UnboundLocalError`.

 **Приклад, який спричинить помилку:**

```python
 global_var = 5

def my_function():
 print(global_var) # Спроба прочитати
 global_var = 10   # Присвоєння (робить 'global_var' локальною для функції)
```
### my_function()  <- Виклик тут призведе до UnboundLocalError

 *Помилка виникає, оскільки присвоєння `global_var = 10` змусило Python вважати `global_var` локальною змінною, 
 а локальна змінна не може бути прочитана до її створення.*

Для коректної зміни глобальної змінної використовуйте **`global`** (як було згадано на лекції):

```python
global_var = 5
def my_function_fixed():
    global global_var # Явно вказуємо, що працюємо з глобальною змінною
    print(global_var)
    global_var = 10
```

-----

### 3. 🎯 Типізація та анотації (Type Hints)

Хоча в лекції згадувалося, що це буде окрема тема, важливо пам'ятати, що сучасний Python використовує анотації типів 
(Type Hints):

  * **Суть:** Використання `:` та `->` для вказівки очікуваних типів аргументів функції та типу значення, яке вона повертає.
  * **Чому важливо:** Покращує **читабельність коду**, допомагає автоматичним інструментам (лінтерам, IDE, MyPy) **статично перевіряти код** на помилки до його запуску.

 **Приклад:**

```python
 def calculate_area(width: int, height: int) -> int:
     """Розраховує площу прямокутника."""
     return width * height
```

Це забезпечує більшу ясність і якість коду, що є ключовою метою, яка підкреслювалася в лекції (відступи, документація).

-----
