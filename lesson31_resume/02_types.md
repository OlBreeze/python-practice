Расскажу подробно и понятно, чтобы всё легло по полочкам. Ниже — расширенное объяснение всех упомянутых категорий типов, их особенности и примеры использования.

---

# 🧩 **2. Типы данных в Python — подробности**

Python — язык с **динамической типизацией**, то есть тип переменной определяется во время выполнения, а не заранее.
Каждый тип — это класс, а значения — экземпляры этих классов.

---

# **2.1 Категории типов данных**

## 🔢 **Числовые типы**

### 1) **int**

Целые числа любого размера.

```python
a = 5
b = -100
c = 10_000_000  # Подчёркивание — просто для удобства
```

### 2) **float**

Числа с плавающей точкой (вещественные).

```python
x = 3.14
y = -0.00001
```

### 3) **complex**

Комплексные числа с частью `real + imaginaryj`.

```python
z = 3 + 4j
```

Используется редко — в научных вычислениях.

---

## 📝 **Текстовый тип**

### **str**

Строки — неизменяемые последовательности Unicode-символов.

```python
s = "Hello"
print(s[0])    # 'H'
```

Неизменяемость означает, что `s[0] = 'X'` вызвать ошибку.

---

## 🟩 **Логический тип**

### **bool**

Может быть только:

* `True`
* `False`

Но на самом деле в Python — это подтип `int` (True = 1, False = 0).

---

## 📦 **Коллекции-последовательности**

### 1) **list**

Изменяемая последовательность.

```python
lst = [1, 2, "hello"]
lst.append(4)
```

Особенности:

* изменяемый
* упорядоченный
* индексация и срезы

---

### 2) **tuple**

Неизменяемая последовательность.

```python
t = (1, 2, 3)
```

Используют, когда нужно гарантирующее постоянство данных или передача неизменяемых наборов.

---

### 3) **range**

Диапазон чисел — ленивый объект.

```python
r = range(1, 5)  # 1,2,3,4
```

Не хранит все числа, а генерирует по необходимости.

---

## 🧺 **Коллекции-наборы**

### 1) **set**

Неупорядоченный набор уникальных элементов.

```python
s = {1, 2, 3, 3}   # {1,2,3}
```

---

### 2) **frozenset**

То же самое, что set, но **неизменяемый**.

---

### 3) **dict**

Словарь — хранит пары «ключ — значение».

```python
d = {"name": "Olga", "age": 30}
```

Особенности:

* с Python 3.7 — сохраняет порядок добавления
* доступ по ключу за O(1)

---

## 🎯 **Специальные типы**

### **type**

Используется реже, но важен: это тип типов.

```python
x = 5
print(type(x))   # <class 'int'>
print(type(int)) # <class 'type'>
```

---

# **2.2 Классификация последовательностей**

## 🧬 Изменяемые vs Неизменяемые

### 🔹 **Изменяемые (mutable):**

* list
* dict
* set

Можно менять содержимое:

```python
lst = [1, 2]
lst[0] = 100
```

### 🔹 **Неизменяемые (immutable):**

* str
* tuple
* frozenset

Изменение создаёт новый объект:

```python
s = "hi"
s = s + "!"
```

---

## 📏 Упорядоченные vs Неупорядоченные

### **Упорядоченные**

Имеют фиксированный порядок элементов:

* list
* tuple
* string
* range
* dict (сохраняет порядок, но доступ по индексу невозможен)

Можно обращаться по индексу:

```python
lst[0]
```

### **Неупорядоченные**

* set
* frozenset

Порядок не гарантируется, индексов нет.

---

# 📚 Дополнительные полезные моменты

## ❗ NoneType

Тип специального значения None.

```python
x = None
```

---

## 🔢 Bytes, bytearray

Используются для работы с бинарными данными.

```python
b = bytes([1,2,3])
ba = bytearray([1,2,3])  # изменяемый вариант
```

---

## 🧊 Важное про типы

* Все типы — **классы**
* Всё в Python — **объект**
* Типы могут иметь свои методы (`"hello".upper()`)

---

Отлично! Продолжаем разбирать типы данных в Python, но теперь глубже и практичнее. Ниже — несколько важных концепций, которые сильно помогают понимать работу языка «под капотом».

---

# 🔍 **3. Хэшируемость (hashable) — что это и почему важно**

## ✔ Что значит «объект хэшируемый»?

Объект хэшируемый, если:

1. **имеет хэш (hash())**
2. **его хэш не меняется во время жизни объекта**
3. **он сравнивается по значению (==), а не по ссылке**

Примеры хэшируемых типов:

* int
* float
* str
* tuple (если все элементы внутри тоже хэшируемые)
* frozenset

Нехэшируемые:

* list
* dict
* set

## ✔ Почему list нельзя положить в set?

Потому что list изменяемый — его хэш не может быть постоянным.

```python
s = {1, 2, (3, 4)}     # ok
s = { [1, 2, 3] }       # ❌ TypeError: unhashable type: 'list'
```

---

# 🧱 **4. Модель памяти Python: изменяемые vs неизменяемые**

Python работает по принципу:

**Изменяемые объекты меняют состояние
Неизменяемые — создают новый объект**

### Пример со строками:

```python
s = "hi"
print(id(s))
s = s + "!"
print(id(s))
```

ID будет разный → создан новый объект.

### Пример со списком:

```python
lst = [1, 2]
print(id(lst)) 
lst.append(3)
print(id(lst))
```

ID совпадает → объект изменился.

---

# 🧩 **5. Как работать с типами в реальной разработке**

## ✔ Посмотреть все методы типа

```python
dir(str)
dir(list)
```

## ✔ Узнать тип переменной

```python
type(x)
```

## ✔ Проверить принадлежность типу

```python
isinstance(10, int)          # True
isinstance([1,2], (list, tuple))
```

---

# ⚙️ **6. Особенности конкретных типов**

## 🧵 **Строки (str)**

* неизменяемые
* поддерживают много методов: `.split()`, `.replace()`, `.upper()`
* поддерживают срезы

---

## 📚 **Списки (list)**

Один из самых мощных типов:

* `.append()`
* `.extend()`
* `.insert()`
* `.remove()`
* `.pop()`
* сортировка: `.sort()` или `sorted()`

---

## 🗂 **Словари (dict)**

* доступ по ключу за O(1)
* поддерживают полезные методы: `.get()`, `.keys()`, `.values()`, `.items()`

Важно: словари **упорядоченные**, но **индекса нет**.

```python
d = {"a": 1, "b": 2}
# d[0]  ❌ нельзя, это не список
```

---

## 🎯 **Кортежи (tuple)**

Почему кортежи полезны?

* быстрее списка
* неизменяемые
* можно использовать как ключ dict

Из-за неизменяемости tuple более надёжен, чем list.

---

# ⚡ **7. Преобразование типов**

Частое действие в Python.

### Примеры:

```python
int("123")       # 123
float("3.14")    # 3.14
str(10)          # "10"
list("hello")    # ['h','e','l','l','o']
tuple([1,2,3])   # (1,2,3)
set([1,2,2,3])   # {1,2,3}
```

---

# 🚀 **8. Полная таблица типов Python**

| Категория          | Тип       | Изменяемость | Упорядоченность | Хэшируемость |
| ------------------ | --------- | ------------ | --------------- | ------------ |
| Числа              | int       | ❌            | —               | ✔            |
|                    | float     | ❌            | —               | ✔            |
|                    | complex   | ❌            | —               | ✔            |
| Текст              | str       | ❌            | ✔               | ✔            |
| Логический         | bool      | ❌            | —               | ✔            |
| Последовательности | list      | ✔            | ✔               | ❌            |
|                    | tuple     | ❌            | ✔               | ✔*           |
|                    | range     | ❌            | ✔               | ✔            |
| Наборы             | set       | ✔            | ❌               | ❌            |
|                    | frozenset | ❌            | ❌               | ✔            |
| Словари            | dict      | ✔            | ✔               | ❌            |
| Специальные        | NoneType  | ❌            | —               | ✔            |
|                    | type      | ❌            | —               | ✔            |

* tuple — хэшируемый, если все его элементы хэшируемые.

---



