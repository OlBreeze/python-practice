# Структурированная лекция по Python: Подготовка к технической собеседке

## 1. Основы Python

### 1.1 Что такое Python?
- **Многопарадигменный язык программирования**
  - ООП (объектно-ориентированное программирование)
  - Функциональное программирование
  - Императивное программирование (линейные алгоритмы)

### 1.2 Применение Python
- Backend-разработка (Django, Flask, FastAPI)
- Математические задачи и работа с большими данными
- Микросервисная архитектура
- Машинное обучение и тренировка моделей
- Многопоточность и многопроцессорность
- Прямая работа с железом

---

## 2. Типы данных

### 2.1 Категории типов данных
- **Числовые**: integer, float, complex
- **Текстовые**: string
- **Логические**: bool
- **Коллекции-последовательности**: list, tuple, range
- **Коллекции-наборы**: dict, set, frozenset
- **Специальные**: type

### 2.2 Классификация последовательностей
**Изменяемые (mutable) vs Неизменяемые (immutable)**
- Изменяемые: list, dict, set
- Неизменяемые: tuple, string, frozenset

**Упорядоченные vs Неупорядоченные**
- Упорядоченные: list, tuple, string, range
- Неупорядоченные: set (не гарантируют порядок, нет доступа по индексу)

---

## 3. Хеширование

### 3.1 Что такое хеш?
- Числовое значение фиксированной длины
- Генерируется хеш-функцией
- Уникальное значение для объекта

### 3.2 Применение
- Используется в словарях (dict) и множествах (set)
- Определяет конкретную ячейку памяти (bucket)
- Обеспечивает доступ со сложностью O(1)
- Не требует последовательной проверки каждого элемента

---

## 4. Функции

### 4.1 Определение
- Именованный и многократно используемый блок кода
- Избегаем дублирования кода (не питонично)

### 4.2 Преимущества функций
1. **Инкапсуляция** - объединение инструкций как единого целого
2. **Организованный код** - лучшая структура
3. **Параметризация** - прием входных данных
4. **Возврат результата** - выходные значения
5. **Test-Driven Development (TDD)** - легче реализовать юнит-тестирование

### 4.3 Виды функций
- Встроенные (built-in)
- Пользовательские
- Анонимные (lambda)
- Методы
- Процедуры (без return)

---

## 5. Range

### 5.1 Характеристики
- Тип данных, который является immutable
- Функция-генератор последовательностей
- Создает последовательность целочисленных значений

### 5.2 Использование
```python
range(10)           # от 0 до 9 (не включительно)
range(5, 10)        # от 5 до 9
range(5, 10, 2)     # от 5 до 9 с шагом 2
```

---

## 6. Итераторы и Генераторы

### 6.1 Итератор
- Объект, реализующий протокол итератора через `__iter__` и `__next__`
- Состояние хранится во внутренних переменных класса
- Явное создание через класс или встроенные функции

### 6.2 Генератор
- Специальная функция, возвращающая объект-итератор
- Неявное создание через `yield` или генераторное выражение
- Автоматическое сохранение состояния
- "Замораживает" выполнение на операторе `yield`

### 6.3 Применение
- Обработка входных данных без блокировки программы
- Асинхронность (до Python 3.5 использовались корутины)
- Контекстные менеджеры

---

## 7. Асинхронность

### 7.1 Async/Await
**`async def`** - объявление корутины
- Функция не выполняется немедленно
- Возвращает объект корутины
- Требует передачи в event loop для выполнения

**`await`** - синтаксический сахар
- Приостанавливает выполнение текущей корутины
- Передает управление обратно в event loop
- Event loop может взять другую готовую корутину

### 7.2 Event Loop (Цикл событий)
**Проверяет три вещи:**
1. Какие корутины ожидают/заблокированы на await
2. Какие внешние операции завершились (сеть вернула данные)
3. Какие корутины готовы к выполнению

**Преимущества:**
- Быстрое переключение контекста между корутинами
- Выполнение активных, игнорирование приостановленных

### 7.3 Корутины vs Потоки
- **Корутины**: эффективный I/O, тысячи одновременных сетевых запросов
- **Многопоточность**: параллельное выполнение CPU-интенсивных задач

---

## 8. Метаклассы

### 8.1 Определение
- "Класс классов"
- Позволяют перехватывать и модифицировать процесс создания класса
- Динамическое изменение, валидация, добавление методов/атрибутов

### 8.2 Процесс работы
1. Python встречает определение класса
2. Ищет метакласс (по умолчанию `type`)
3. Использует указанный метакласс или `type`

---

## 9. Dependency Injection (Внедрение зависимостей)

### 9.1 Суть
- Отделение объекта от его зависимостей
- Внешний механизм предоставляет зависимости
- Объект не создает зависимости сам

### 9.2 Применение
- Логирование (внедрение зависимости в логгер)
- Тестирование (мокирование - замена реальных зависимостей на контролируемые)
- Класс становится независимым от конкретной реализации
- Уменьшение связанности

---

## 10. Идемпотентность

### 10.1 Определение
- Свойство операции: многократное использование = одноразовое использование
- После первого успешного вызова последующие не меняют состояние системы

### 10.2 Идемпотентные HTTP-методы
- **GET**: получение данных, не меняет состояние
- **HEAD**: получение заголовков, не меняет состояние
- **PUT**: полное обновление (повторное обновление = тот же результат)
- **DELETE**: после первого удаления ресурса повторные запросы не меняют факт отсутствия

### 10.3 Неидемпотентные методы
- **POST**: создает новый ресурс, каждый вызов создает новую запись

---

## 11. Оптимизация в Python

### 11.1 Профилирование
"Невозможно оптимизировать то, что не измерено"

**Speed Profiling:**
- **cProfile** - стандартный, самый точный модуль
- Находит самые медленные функции в большом приложении

**Memory Profiling:**
- **memory_profiler** - находит объекты, занимающие больше всего памяти
- Выявляет утечки памяти

### 11.2 `__slots__` vs `__dict__`

**По умолчанию:**
- Каждый экземпляр класса имеет `__dict__`
- Хранит все атрибуты, можно динамически добавлять

**`__slots__`:**
- Резервирует фиксированное место для атрибутов
- Не создается словарь

**Преимущества `__slots__`:**
1. Экономия памяти (нет словаря)
2. Нельзя динамически добавлять атрибуты
3. Быстрый доступ (фиксированное смещение vs поиск в хеш-таблице)

---

## 12. Метрики для API

### 12.1 Производительность
- **Latency** - время отклика (от запроса до ответа)
- **Время выполнения запросов к БД**
- **Время кеша** (чтение/запись)
- **Пропускная способность** - запросов в секунду

### 12.2 Ресурсы
- Загрузка сервера (CPU, RAM)
- Активные соединения с БД
- Использование пула соединений
- Hit rate кеша (из кеша vs из БД)

### 12.3 Celery (если используется)
- Активные/запланированные задачи
- Время выполнения задач

### 12.4 Надежность
- Логирование
- Успешные запросы vs ошибки
- Ошибки клиента (4xx) vs сервера (5xx)

---

## 13. GIL (Global Interpreter Lock)

### 13.1 Что такое GIL?
- Механизм в CPython
- В любой момент времени только ОДИН поток выполняет байт-код Python

### 13.2 Причины существования
1. **Управление памятью** - подсчет ссылок на объекты
2. **Предотвращение гонки данных** - защита счетчика ссылок
3. **Простая реализация** - не нужны сложные блокировки на каждом объекте

### 13.3 Обход ограничений GIL

**1. Асинхронность (asyncio)**
- Один поток, GIL не создает проблем
- Эффективная обработка I/O через приостановку корутин

**2. Мультипроцессорность (multiprocessing)**
- Каждый процесс имеет собственный интерпретатор Python
- Каждый процесс имеет собственный GIL
- Настоящий параллелизм на нескольких ядрах CPU

**Почему asyncio не решает проблему?**
- Использует один поток
- GIL не создает проблем при работе с asyncio

---

## 14. Инкапсуляция

### 14.1 Механизмы реализации
1. **Модификаторы доступа**
   - Public (публичный)
   - Protected (защищенный) - `_attribute`
   - Private (приватный) - `__attribute`

2. **Property** - декоратор для getter/setter

3. **Дескрипторы** - объекты, реализующие протокол дескриптора

### 14.2 Дескрипторы

**Что это?**
- Объект, реализующий `__get__`, `__set__`, `__delete__`
- Перехватывает операции доступа к атрибутам

**Преимущества над property:**
- Property работает индивидуально с каждым атрибутом
- Дескриптор - для общей логики валидации
- Пример: возраст, количество товаров - всегда положительные числа
- Можно создать дескриптор и использовать его многократно

---

## 15. Сборщик мусора (Garbage Collector)

### 15.1 Две стратегии

**1. Подсчет ссылок (Reference Counting)**
- Каждый объект имеет счетчик ссылок
- **Увеличение**: присваивание переменной, передача как аргумент, добавление в контейнер
- **Уменьшение**: выход из области видимости, переприсваивание, удаление из контейнера
- **Счетчик = 0**: память немедленно освобождается

**Проблема**: циклические ссылки (два объекта ссылаются друг на друга)

**2. Генерационный сборщик мусора**
Гипотеза: чем дольше объект существует, тем меньше вероятность стать мусором

**Три поколения:**
- **Generation 0** (молодое):
  - Новые объекты
  - Самая высокая частота проверки
  - Короткоживущие объекты (временные переменные)

- **Generation 1** (среднее):
  - Объекты, пережившие одну проверку Gen 0
  - Умеренная частота проверки

- **Generation 2** (старое):
  - Объекты, пережившие проверку Gen 1
  - Самая низкая частота проверки
  - Долгоживущие объекты (основная структура приложения)

### 15.2 Слабые ссылки (Weak References)
- Ссылка на объект, НЕ увеличивающая счетчик ссылок
- Позволяет хранить ссылку, не мешая сборщику мусора удалить объект

---

## 16. Кортежи (Tuple)

### 16.1 Характеристики
- Неизменяемый тип данных (immutable)
- Нельзя изменить количество элементов
- НО: если элемент внутри кортежа изменяемый (список), его можно изменить

---

## 17. Гибридный подход: asyncio + multiprocessing

### 17.1 Проблема
Высокопроизводительный сервис обрабатывает:
- CPU-bound задачи (обработка изображений)
- I/O-bound задачи (сетевые запросы)

### 17.2 Решение
1. **Разделить задачи**:
   - I/O задачи в один файл
   - CPU задачи в другой файл

2. **Asyncio** - основной фреймворк для сетевых запросов

3. **Мультипроцессинг** - для сложных задач
   - Выгрузка в отдельный пул процессов
   - Не блокируется event loop

4. **`run_in_executor`**:
   - Метод event loop
   - Планирует синхронные блокирующие функции
   - Выполнение в пуле потоков/процессов
   - Не блокирует главный поток

---

## 18. Протоколы и низкоуровневые механизмы

### 18.1 Итератор
- Объект, представляющий поток данных
- Позволяет перемещаться по элементам контейнера
- Паттерн проектирования: скрывает детали реализации
- Пример: `filter()` возвращает list_iterator

### 18.2 Использование
- Перебор через цикл `for`
- Преобразование через `list()` или `tuple()`

---

## 19. Кеширование

### 19.1 Типы кеширования

**1. In-Memory Cache (декоратор @cache)**
- Кеш в оперативной памяти процесса
- **Скорость**: самый быстрый доступ
- **Масштабируемость**: низкая (уникален для каждого процесса)
- **Стойкость**: отсутствует (данные теряются при перезапуске)
- **Ограничение**: RAM процесса
- **Проблема**: холодный кеш (каждый экземпляр заполняет сам)

**2. Distributed Cache (Redis, Memcached)**
- Распределенное кеширование в оперативной памяти
- **Скорость**: очень быстрая (микросекунды), но требует сетевого запроса
- **Масштабируемость**: высокая (доступен всем микросервисам)
- **Стойкость**: Redis может сохранять на диск
- **Преимущества**: 
  - Отдельный сервер (не влияет на память приложения)
  - Общий доступ для всех инстансов
- **Недостатки**: внешняя зависимость, сетевая задержка

**3. Database Caching**
- Для очень больших объемов данных (big data)

### 19.2 Факторы выбора
1. Размер кеша и частота устаревания данных
2. Стойкость (должны ли данные переживать перезапуск)
3. Стоимость вычислений
4. Доступность для нескольких экземпляров

### 19.3 Рекомендации
- **Монолитная архитектура** + небольшие данные → In-Memory Cache
- **Микросервисная архитектура** + много экземпляров → Distributed Cache (Redis)

---

## 20. Snapshot Testing

### 20.1 Что это?
- Методология тестирования
- Выходной результат сохраняется как "снимок" при первом запуске
- Последующие запуски сравниваются с этим снимком

### 20.2 Процесс
1. **Первый запуск**: рендеринг компонента → сериализация → сохранение в файл
2. **Последующие запуски**: новый результат сравнивается со снимком
3. **Тест проходит**: если результаты идентичны
4. **Тест проваливается**: если результаты различаются

### 20.3 Преимущества и недостатки
**✓ Преимущества:**
- Сразу видна регрессия в структуре данных
- Обеспечивает неизменность структуры

**✗ Недостатки:**
- Тестирует только выходной результат, не корректность
- Если начальный снимок ошибочный, все тесты бессмысленны

---

## 21. `__new__` vs `__init__`

### 21.1 `__new__`
- **Отвечает за**: создание нового объекта
- **Принимает**: класс как первый аргумент + другие аргументы конструктора
- **Возвращает**: экземпляр класса (через `super().__new__()`)
- **Применение**: метаклассы, паттерн Singleton

### 21.2 `__init__`
- **Отвечает за**: инициализацию созданного объекта
- **Назначение**: установка начальных значений атрибутов
- **Вызывается**: вторым (после успешного `__new__`)

### 21.3 Когда переопределять `__new__`?
- Контроль создания самого объекта
- Паттерн Singleton (единственный экземпляр класса)
- Для большинства классов не переопределяется

---

## 22. Тестирование: Mocking, Stubbing, Spying

### 22.1 Определения

**Mock (Имитатор)**
- **Роль**: проверка поведения
- **Что делает**: имитирует, предоставляет ответы, регистрирует вызовы
- **Проверяет**: сколько раз вызван, с какими аргументами
- **Когда**: проверка взаимодействия между объектами

**Stub (Заглушка)**
- **Роль**: предоставление данных
- **Что делает**: возвращает заранее определенные ответы
- **Проверяет**: правильность обработки входных данных
- **Когда**: тест фокусируется на проверке логики

**Spy (Шпион)**
- **Роль**: частичная проверка
- **Что делает**: оборачивает реальный объект, вызывает реальные методы
- **Проверяет**: корректность вызова реального метода
- **Когда**: верификация поведения существующего объекта

### 22.2 Всегда ли мокать внешние зависимости?

**Юнит-тесты**: ДА
- Внешние зависимости (БД, API) мокаются
- Обеспечивает изоляцию и скорость

**Интеграционные тесты**: НЕТ
- Проверка реальной взаимодействия
- Реальная БД (запущенная локально)

**Рекомендации:**
- Не злоупотреблять моками (признак плохого дизайна)
- Использовать stubs, фокусироваться на поведении

---

## 23. Streaming API

### 23.1 Определение
- Передача данных в виде постоянного непрерывного потока

### 23.2 Технологии

**WebSockets:**
- Постоянное двустороннее (full-duplex) соединение
- Рукопожатие при создании соединения
- Работает в режиме постоянного прослушивания
- Обновление в режиме реального времени (количество пользователей онлайн, новые сообщения)

---

## 24. Модули и пакеты

### 24.1 Модуль
- Любой файл с расширением `.py`
- Содержит функции, классы и/или переменные
- Можно импортировать в другие файлы

### 24.2 Пакет
- Способ группировки связанных модулей
- Иерархическая структура каталогов
- Один или более файлов-модулей в папке
- Специальный файл `__init__.py` обозначает каталог как пакет

---

## 25. Внутренняя структура Dictionary

### 25.1 Хеш-таблицы
**Структура:**
- Кешированное хеш-значение ключа
- Объект-ключ (неизменяемый)
- Значение (value)

**Процесс добавления:**
1. Берется ключ
2. Вычисляется его хеш-значение
3. Хеш определяет индекс позиции во внутреннем массиве

### 25.2 `OrderedDict` vs обычный `dict`

**Обычный `dict` (Python 3.7+):**
- Запоминает порядок добавления элементов
- При итерации возвращает в том же порядке

**`OrderedDict`:**
- Специализированный класс
- Специальные методы для перемещения элементов
- **`popitem(last=True/False)`** - удаление с конца/начала
- **`move_to_end(key)`** - перемещение в конец/начало
- Часто используется для реализации LRU-кеша

---

## 26. Паттерн Circuit Breaker (Автоматический выключатель)

### 26.1 Назначение
- Повышение отказоустойчивости в микросервисах
- Предотвращает постоянные попытки вызова неисправного сервиса
- Дает время несправному сервису на восстановление

### 26.2 Принцип работы

**Три состояния:**

1. **Closed (Замкнутый)** - нормальное состояние
   - Запросы проходят
   - Если количество ошибок превышает лимит → переход в Open

2. **Open (Открытый)**
   - Запросы немедленно отклоняются (503 или кешированная ответ)
   - Не ждет timeout
   - Через определенное время → Half-Open

3. **Half-Open (Полуоткрытый)**
   - Пропускает ограниченное количество тестовых запросов
   - Если успешны → возврат в Closed
   - Если нет → возврат в Open

### 26.3 Преимущества перед retry

**Обычный retry:**
- Агрессивно усиливает нагрузку на неисправный сервис
- Может ухудшить проблему

**Circuit Breaker:**
- **Самозащита**: дает время на восстановление
- **Меньше задержки**: не нужно ждать timeout

### 26.4 Реализация в Python
- **pybreaker**
- **tenacity** - фокус на retry с настройкой

---

## 27. MRO (Method Resolution Order)

### 27.1 Определение
- Порядок разрешения методов
- Алгоритм определения порядка поиска методов при множественном наследовании
- Гарантирует выполнение наиболее релевантной реализации только один раз

### 27.2 Получение MRO
- **`ClassName.__mro__`** (атрибут) → возвращает кортеж
- **`ClassName.mro()`** (метод) → возвращает список

### 27.3 Проблема диаманта (Diamond Problem)

**Структура:**
```
    A (базовый)
   / \
  B   C
   \ /
    D
```

**Проблема:**
- D наследуется от B и C
- B и C наследуются от A
- При вызове метода в D: откуда брать реализацию (из B или C)?
- Неопределенный порядок приоритета

**Решение:** MRO определяет четкий порядок поиска

---

## 28. Базы данных

### 28.1 Транзакция
- Единица работы над базой данных
- Один или несколько запросов
- Рассматривается как атомарная операция (неделимая)
- Определенный перечень команд для достижения цели

### 28.2 MVCC (Multiversion Concurrency Control)

**Что это?**
- Многоверсионный контроль параллельности
- Метод обеспечения изоляции транзакций

**Принцип работы:**
1. При записи транзакция НЕ перезаписывает старые данные
2. Создается новая версия данных
3. Читающие транзакции видят старую консистентную версию
4. Одновременные операции чтения и записи не блокируют друг друга

**Используется в:** PostgreSQL

---

## 29. MongoDB: Шардинг и индексация

### 29.1 Шардинг
- **Горизонтальное масштабирование**
- Распределение больших датасетов на несколько независимых серверов (шардов)
- Обрабатывает больше данных, выше скорость запросов

### 29.2 Шард-ключ
- Поле или комбинация полей для распределения данных
- Нельзя изменить после шардирования коллекции

### 29.3 Когда нужен шардинг?
- Данные превышают емкость одного сервера (память/диск)

### 29.4 Риски неправильного выбора ключа

**1. Горячий шард**
- Ключ с низкой кардинальностью (мало уникальных значений)
- Пример: поле "страна"
- Большинство запросов попадает на один шард → перегрузка
- Другие шарды простаивают

**2. Range queries**
- Если ключ выбран так, что диапазонные запросы требуют проверки всех шардов
- Эффективность шардинга теряется

---

## 30. Паттерны проектирования

### 30.1 Стратегия vs Фабричный метод

**Стратегия (Поведенческий):**
- **Цель**: динамически менять поведение объекта во время выполнения
- **Роль объекта**: имеет поведение

**Фабричный метод (Порождающий):**
- **Цель**: делегировать создание объекта подклассам
- **Роль объекта**: создает объект

### 30.2 Декоратор vs Адаптер

**Декоратор:**
- **Цель**: расширение функциональности объекта динамически
- **Применение**: логирование, валидация, кеширование

**Адаптер:**
- **Цель**: преобразование интерфейса объекта
- **Применение**: совместимость с другим ожидаемым интерфейсом

**Пример совместного использования (E-commerce):**
- Декоратор: кеширование данных
- Адаптер: интеграция с API платежного шлюза с несовместимым интерфейсом

### 30.3 Медиатор vs Наблюдатель

**Наблюдатель (Observer):**
- **Связь**: один ко многим (one-to-many)
- **Компоненты**: наблюдатели независимы
- **Пример**: изменение статуса заказа → обновление сервиса уведомлений, логирования, системы склада
- **Связь**: децентрализованная (субъект не знает, кто наблюдает)

**Медиатор (Mediator):**
- **Связь**: многие ко многим (many-to-many)
- **Компоненты**: зависят от посредника
- **Пример**: диалоговое окно (кнопки, поля не общаются напрямую, только через медиатор)
- **Связь**: централизованная (все компоненты знают о посреднике)

### 30.4 Абстрактная фабрика vs Строитель (Builder)

**Абстрактная фабрика:**
- Создает семейство связанных объектов

**Строитель (Builder):**
- Поэтапное конструирование одного сложного объекта
- Много опций и разных репрезентаций

**Когда Builder лучше:**
1. **Разные репрезентации**: процесс одинаковый, результат разный (HTML page vs PDF document)
2. **Сложный конструктор**: десятки необязательных параметров
3. **Читабельность**: установка параметров через методы (только нужные)

---

## 31. Паттерн Saga

### 31.1 Проблема
- В микросервисах нельзя использовать традиционные ACID транзакции
- Двухфазный commit не подходит для гарантии целостности данных

### 31.2 Решение
- Последовательность локальных транзакций
- Каждый микросервис выполняет свою часть
- При неудаче любой транзакции → компенсирующие транзакции в обратном порядке
- Гарантирует финальную согласованность

### 31.3 Реализация

**1. Хореография:**
- Сервисы общаются через брокер сообщений (Kafka, RabbitMQ)
- Сервис-отправитель публикует событие
- Запускает компенсационную логику

**2. Оркестровка:**
- Центральный сервис-оркестратор (на Python)
- Отвечает за вызов и мониторинг всех локальных транзакций
- При неудаче → вызывает компенсирующие транзакции

---

## 32. Партиционирование таблиц vs Шардинг

### 32.1 Партиционирование таблиц
- **Масштабирование**: вертикальное (один сервер)
- **Что делает**: логическое разделение одной таблицы на части
- **Преимущества**:
  - Меньшие индексы → лучше производительность
  - Упрощение обслуживания
  - Быстрое удаление/архивирование старых данных
- **Когда использовать**: очень большие таблицы на одном сервере

### 32.2 Шардинг
- **Масштабирование**: горизонтальное (несколько серверов)
- **Что делает**: горизонтальное разделение всей БД на независимые серверы
- **Преимущества**:
  - Преодоление ограничений одного сервера
  - Высокая отказоустойчивость
  - Возможность добавлять новые серверы
- **Когда использовать**: превышение лимита одного сервера, значительный рост данных

---

## 33. MongoDB: Оптимизация Aggregation Pipeline

### 33.1 Проблема
- Сложные агрегации требуют много памяти
- Лимит по умолчанию: 100MB на этап
- При превышении → ошибка

### 33.2 Три основные практики оптимизации

**1. Включить allowDiskUse: true**
- Использование временных файлов на диске для промежуточных данных
- Обходит лимит памяти

**2. Ранняя фильтрация (Early Filtering)**
- Уменьшение количества документов на ранних этапах
- Меньше данных попадает в ресурсоемкие этапы
- Снижение использования памяти и CPU

**3. Оптимизация сортировки**
- Добавление ограничений (limit)
- Сокращение объема сортируемых данных

---

## 34. CQRS (Command Query Responsibility Segregation)

### 34.1 Что это?
- Разделение (сегрегация) ответственности за операции чтения и записи

### 34.2 Отличия от CRUD

**CRUD:**
- Одна модель для чтения и записи

**CQRS:**
- **Команды (Commands)** - модель записи, изменение состояния
- **Запросы (Queries)** - модель чтения, получение данных для отображения/отчетов

### 34.3 Преимущества
- Решает проблемы масштабирования
- Оптимизация для разных типов операций

---

## 35. Zero Trust Architecture

### 35.1 Концепция
- "Никому не доверяй, всегда проверяй"
- Каждый запрос (внутри/снаружи) должен быть проверен, авторизован и зашифрован

### 35.2 Реализация

**1. Ограничение доступа на уровне инфраструктуры**
- Сервис A может общаться только с Сервисом B (если нужно)

**2. Аутентификация**
- JWT (JSON Web Token) или OAuth 2.0
- Проверка идентичности клиента/пользователя/сервиса на каждом endpoint

**3. Middleware в FastAPI/Django**
- Валидация токена в заголовке
- Проверка срока действия и подписи
- Проверка прав доступа перед обработкой запроса

### 35.3 Управление секретами
**Недопустимо:**
- Хранение секретов в коде
- Хранение в переменных окружения

**Правильно:**
- AWS Secrets Manager
- Azure Key Vault
- Kubernetes Secrets

**Практика:**
- Загрузка секретов при старте сервиса
- Хранение в оперативной памяти
- Избегать записи на диск

---

## 36. Copy-on-Write (COW)

### 36.1 Что это?
- Стратегия оптимизации управления памятью в операционных системах

### 36.2 Принцип работы

**При форкинге процесса:**
1. Предок и потомок изначально используют одну физическую память (страницы)
2. Копирование страницы происходит ТОЛЬКО при попытке записи
3. ОС создает приватную копию для записывающего процесса

### 36.3 Применение в multiprocessing

**Условия:**
- Использование метода fork (MacOS, Linux)
- Создание процесса через класс `multiprocessing.Process`

**Преимущества:**
- Экономия памяти (общие неизменяемые структуры)
- Быстрый старт процессов
- Быстрое чтение больших структур данных

**Когда снижает производительность:**
- Потомок сразу записывает/изменяет большую часть данных
- Создается много копий страниц
- Затримка и высокое потребление памяти

---

## Заключение

Эта лекция охватывает ключевые темы для подготовки к технической собеседке на позицию Python-разработчика. Рекомендуется:

1. Повторить вопросы, вызвавшие затруднения
2. Изучить более сложные темы (Saga, CQRS, Zero Trust)
3. Подготовить комплексный проект (Django/FastAPI + асинхронность + Telegram API)
4. Задеплоить проект и добавить в портфолио

Удачи на собеседованиях! 🚀