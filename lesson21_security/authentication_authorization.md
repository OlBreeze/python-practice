У Django передбачено кілька способів **аутентифікації та авторизації користувачів**, які можна розділити на **вбудовані механізми** та **зовнішні (через додаткові бібліотеки)**.
Ось основні варіанти 👇

---

### 🧩 **1. Стандартна аутентифікація Django (сесійна)**

🔹 **Як працює:**
Після входу користувача Django створює **сесію** (cookie `sessionid`), за допомогою якої зберігається стан входу.
Аутентифікація виконується через `django.contrib.auth`.

🔹 **Переваги:**

* Простота реалізації (вбудовано у фреймворк).
* Добре підходить для звичайних вебсайтів із формами логіну.
* Працює з усіма middleware Django.
* Має готову адмінку, групи, права доступу.

🔹 **Недоліки:**

* Не підходить для REST API або SPA (бо працює через cookie).
* Потрібен стан сесії на сервері → не масштабована у мікросервісах.

---

### 🔑 **2. Token Authentication (DRF)**

🔹 **Як працює:**
Користувач отримує **токен** (звичайно довгий рядок), який передає в заголовку кожного запиту:
`Authorization: Token <token>`.

🔹 **Переваги:**

* Простий у використанні з Django REST Framework.
* Не потребує cookies.
* Добре працює з мобільними клієнтами або SPA.

🔹 **Недоліки:**

* Токен статичний (не має терміну дії).
* Якщо токен зламано — його можна використовувати безстроково.
* Не дуже безпечний без HTTPS.

---

### 🧠 **3. JWT Authentication (JSON Web Token)**

(зазвичай через бібліотеку `djangorestframework-simplejwt`)

🔹 **Як працює:**
Після входу сервер видає **access** і **refresh** токени.

* Access-токен живе короткий час (наприклад, 5 хв).
* Refresh-токен можна використати для оновлення access.

🔹 **Переваги:**

* Повністю **статлес** — не потребує сесій на сервері.
* Зручно в мікросервісній архітектурі.
* Термін дії токена обмежений → безпечніше.
* Можна передавати додаткові claims (роль, user_id).

🔹 **Недоліки:**

* Потрібна правильна реалізація перевірки токенів.
* Якщо refresh-токен викрадено — його можна використовувати довше.
* Трохи складніший у налаштуванні.

---

### 🔐 **4. OAuth2 / Social Auth**

(через `django-allauth`, `social-auth-app-django`)

🔹 **Як працює:**
Користувач проходить авторизацію через зовнішній сервіс (Google, Facebook, GitHub).
Сервіс видає access token, який Django використовує для створення або входу користувача.

🔹 **Переваги:**

* Зручно для користувача — не треба створювати пароль.
* Безпечна делегована аутентифікація.
* Підтримує SSO (single sign-on).

🔹 **Недоліки:**

* Залежність від стороннього постачальника.
* Складніша початкова інтеграція.
* Потрібно керувати callback URL та токенами.

---

### ⚙️ **5. Custom Authentication (власні бекенди)**

🔹 **Як працює:**
Можна створити власний клас `AuthenticationBackend`, який перевіряє користувачів, наприклад, по email, токену API, LDAP тощо.

🔹 **Переваги:**

* Гнучкість — можна адаптувати під будь-яку систему.
* Можна комбінувати з іншими видами аутентифікації.

🔹 **Недоліки:**

* Потрібно писати вручну та тестувати.
* Легко зробити помилку в безпеці.

---

### 🔒 **Підсумок:**

| Тип аутентифікації | Підходить для               | Безпечність | Складність |
| ------------------ | --------------------------- | ----------- | ---------- |
| Django Sessions    | Класичні сайти              | ⭐⭐          | ⭐          |
| DRF Token          | Простий REST API            | ⭐⭐          | ⭐⭐         |
| JWT                | SPA, мобільні, мікросервіси | ⭐⭐⭐⭐        | ⭐⭐⭐        |
| OAuth2 / Social    | Зовнішні сервіси, SSO       | ⭐⭐⭐⭐        | ⭐⭐⭐        |
| Custom Backend     | Спеціальні кейси            | залежить    | ⭐⭐⭐⭐       |

---
