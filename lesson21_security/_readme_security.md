# Конспект лекції: Безпека в Django

## 1. Повторення Django REST Framework

### Основні компоненти:
- **Serializers** - перетворювачі моделей у JSON формат
  - Створюються у файлі `serializers.py`
  - Успадковуються від `ModelSerializer`
  - Використовують метаклас для прив'язки до моделі
  - Забезпечують валідацію даних

- **ViewSets** - контролери для обробки запитів
  - Замінюють класичні view
  - Дозволяють кастомізувати відображення об'єктів
  - Використовують `queryset` та `serializer_class`

- **Routers** - маршрутизація
  - `SimpleRouter` - базова маршрутизація
  - `DefaultRouter` - додатково надає кореневе API представлення

- **Permissions** - права доступу
  - Створюються у файлі `permissions.py`
  - Використовуються через атрибут `permission_classes`

## 2. Види веб-вразливостей

### Основні типи атак:
- **Фішинг**
- **SQL-ін'єкції**
- **XSS (Cross-Site Scripting)**
- **CSRF (Cross-Site Request Forgery)** Підробка міжсайтового запиту
- **Clickjacking**  Перехоплення кліків
- **DDoS атаки**
- **Помилки автентифікації**


Ось коротке пояснення кожної з цих загроз 👇

---

### 🎣 **Фішинг (Phishing)**

Атака, під час якої зловмисники видають себе за надійні джерела (наприклад, банки або відомі сервіси), щоб обманом отримати особисті дані користувача — логіни, паролі, номери карток тощо.
📧 Зазвичай здійснюється через підроблені електронні листи або сайти.

---

### 💉 **SQL-ін’єкції (SQL Injection)**

Тип атаки, коли зловмисник вставляє шкідливий SQL-код у поля введення (наприклад, логін або пошук), щоб отримати доступ до бази даних.
⚠️ Може призвести до крадіжки, зміни або видалення даних.

---

### ⚔️ **XSS (Cross-Site Scripting)**

Атака, при якій у вебсторінку вставляється шкідливий JavaScript-код, що виконується у браузері користувача.
😈 Наприклад, може красти cookie-файли або перенаправляти на фальшиві сайти.

---

### 🔁 **CSRF (Cross-Site Request Forgery)**

Зловмисник змушує користувача виконати небажану дію на іншому сайті, де він вже автентифікований (наприклад, переказ грошей або зміну пароля).
🛡️ Захищається токенами CSRF або перевіркою походження запитів.

---
### 🔹 **Clickjacking** (Перехоплення кліків)

Суть: зловмисник змушує користувача натиснути на невидимий елемент справжнього сайту, вставлений у iframe.  
➡️ Користувач думає, що тисне «Play», а натискає «Delete account».  

Механізм: обман з інтерфейсом (UI redress attack).  
Захист: X-Frame-Options, Content-Security-Policy: frame-ancestors.

---

### 🌊 **DDoS атаки (Distributed Denial of Service)**

Масова атака, коли велика кількість запитів надсилається на сервер одночасно, щоб перевантажити його і зробити недоступним для справжніх користувачів.
💥 Мета — паралізувати роботу сайту чи сервісу.

---

### 🔐 **Помилки автентифікації (Authentication Failures)**

Вразливості, що виникають через неправильну реалізацію логіну/реєстрації або управління сесіями.
🙈 Наприклад: зберігання паролів у відкритому вигляді, відсутність перевірки токенів, слабкі паролі.

---

## 3. CSRF захист

### Механізм захисту:
```python
# У шаблоні
<form method="post">
    {% csrf_token %}
    <!-- форма -->
</form>
```

### Налаштування в settings.py:
- Middleware `csrf.CsrfViewMiddleware` (вбудовано)
- Декоратор `@csrf_exempt` (використовувати обережно!)

```python
from django.views.decorators.csrf import csrf_exempt

@csrf_exempt
def my_view(request):
    return JsonResponse({'key': 'value'})
```

## 4. XSS захист

### Автоматичне екранування:
```django
{{ variable }}  <!-- Автоматично екранується -->
{{ variable|safe }}  <!-- Без екранування (тільки для перевірених даних!) -->
```

## 5. SQL-ін'єкції

### Використання ORM:
```python
# Безпечно (через ORM)
Product.objects.all()
Product.objects.filter(price__lt=100)

# НЕБЕЗПЕЧНО (сирий SQL)
cursor.execute(f"SELECT * FROM users WHERE name = '{name}'")

# Безпечно (параметризований запит)
cursor.execute("SELECT * FROM users WHERE name = ?", [name])
```

### Переваги ORM:
- Єдиний синтаксис для різних БД
- Автоматичне екранування запитів
- Мінімізація ризику SQL-ін'єкцій

## 6. Хешування паролів

### Вбудовані функції Django:
```python
from django.contrib.auth.hashers import make_password, check_password

# Хешування
hashed_password = make_password('my_password')

# Перевірка
check_password('my_password', hashed_password)
```

### Алгоритми хешування: (див мануал по смене алгоритма change_PBKDF2 → Argon2.md)
- **PBKDF2** (за замовченням)
- **Argon2** (рекомендовано)

```python
PASSWORD_HASHERS = [
    'django.contrib.auth.hashers.Argon2PasswordHasher',
    'django.contrib.auth.hashers.PBKDF2PasswordHasher',
]
```

**Правило:** НІКОЛИ не зберігати паролі у відкритому вигляді!

## 7. Захист секретних даних через .env

### Встановлення:
```bash
pip install python-dotenv
```

### Використання:
```python
# settings.py
import os
from dotenv import load_dotenv

load_dotenv()

SECRET_KEY = os.getenv('SECRET_KEY')
DEBUG = os.getenv('DEBUG', 'False') == 'True'
```

### Файл .env:
```
SECRET_KEY=your-secret-key-here
DATABASE_PASSWORD=your-db-password
API_KEY=your-api-key
```

**Важливо:** Додати `.env` до `.gitignore`!

## 8. Middleware

### Що таке Middleware:
Проміжний шар між запитом і відповіддю, який обробляє дані до виконання бізнес-логіки.

### Важливість порядку:
Middleware виконуються послідовно згідно з порядком у списку `MIDDLEWARE`

### Вбудовані middleware:
- `SecurityMiddleware`
- `SessionMiddleware`
- `CsrfViewMiddleware`
- `AuthenticationMiddleware`

## 9. Налаштування безпеки в settings.py

### HTTPS та SSL/TLS:
```python
# Примусове перенаправлення на HTTPS
SECURE_SSL_REDIRECT = True

# HTTP Strict Transport Security
SECURE_HSTS_SECONDS = 31536000  # 1 рік
SECURE_HSTS_INCLUDE_SUBDOMAINS = True
SECURE_HSTS_PRELOAD = True

# Заголовки безпеки
SECURE_BROWSER_XSS_FILTER = True
SECURE_CONTENT_TYPE_NOSNIFF = True
X_FRAME_OPTIONS = 'DENY'
```

### Cookies:
```python
# Передача тільки через HTTPS
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True

# Захист від XSS
SESSION_COOKIE_HTTPONLY = True
CSRF_COOKIE_HTTPONLY = True

# Термін життя сесії
SESSION_COOKIE_AGE = 86400  # 24 години (1 доба)
```

### CSRF налаштування:
```python
CSRF_TRUSTED_ORIGINS = [
    'https://your-username.pythonanywhere.com'
]
CSRF_USE_SESSIONS = True
```

### Валідація паролів:
```python
AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]
```

### Обмеження розміру файлів:
```python
DATA_UPLOAD_MAX_MEMORY_SIZE = 2621440  # 2.5 MB
FILE_UPLOAD_MAX_MEMORY_SIZE = 5242880  # 5 MB
```

### Дозволені хости:
```python
ALLOWED_HOSTS = ['your-username.pythonanywhere.com']
DEBUG = False  # У продакшені!
```

## 10. Захист від DDoS та брут-форсу

### Django-axes (обмеження спроб входу):
```bash
pip install django-axes
```

```python
# settings.py
INSTALLED_APPS += ['axes']
MIDDLEWARE += ['axes.middleware.AxesMiddleware']

# Налаштування
AXES_FAILURE_LIMIT = 3  # Кількість спроб
AXES_COOLOFF_TIME = 1  # Годин блокування
AXES_LOCKOUT_BY_USER_OR_IP = True
```

### Throttling (обмеження запитів):
Використання декораторів для обмеження частоти запитів до view.

## 11. Логування

### Базова конфігурація:
```python
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'handlers': {
        'file': {
            'level': 'DEBUG',
            'class': 'logging.FileHandler',
            'filename': BASE_DIR / 'logs/django.log',
        },
    },
    'loggers': {
        'django.security': {
            'handlers': ['file'],
            'level': 'WARNING',
            'propagate': True,
        },
    },
}
```

## 12. Email безпека

```python
EMAIL_USE_TLS = True
EMAIL_USE_SSL = False
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_HOST_USER = os.getenv('EMAIL_USER')
EMAIL_HOST_PASSWORD = os.getenv('EMAIL_PASSWORD')
```

## 13. Content Security Policy (CSP)

```python
CSP_DEFAULT_SRC = ("'self'",)
CSP_SCRIPT_SRC = ("'self'", "'unsafe-inline'")
```

## 14. Декоратори безпеки

### Login required:
```python
from django.contrib.auth.decorators import login_required

@login_required
def my_view(request):
    # Доступно тільки авторизованим користувачам
    pass
```

## Підсумок

### Ключові принципи безпеки:
1. ✅ Використовувати HTTPS/SSL
2. ✅ Хешувати паролі (ніколи не зберігати у відкритому вигляді)
3. ✅ Ховати секретні дані у `.env`
4. ✅ Використовувати ORM замість сирого SQL
5. ✅ Вмикати CSRF захист
6. ✅ Налаштовувати безпечні заголовки
7. ✅ Обмежувати спроби входу
8. ✅ Встановлювати обмеження на розмір файлів
9. ✅ Вимикати DEBUG у продакшені
10. ✅ Налаштовувати логування
11. ✅ Регулярно оновлювати бібліотеки

