
### 🔒 1. Hashing (хешування)

**Хешування** — це процес перетворення пароля у фіксовану строку (хеш) за допомогою односторонньої функції.
Тобто з хешу **неможливо відновити оригінальний пароль**.

🧩 Приклад:

```
Пароль: mySecret123
SHA-256: 8b0a440b54b2e2d3e2d8fef03a2cbbf...
```

Якщо навіть у базу потрапить цей хеш, сам пароль залишиться невідомим.

---

### 🧂 2. Salting (додавання «солі»)

**Сіль (salt)** — це випадковий рядок, який додається до пароля перед хешуванням, щоб ускладнити підбір.

Наприклад:

```
Пароль: mySecret123
Сіль: X9!2kf
Результат хешування: hash("mySecret123X9!2kf")
```

Навіть якщо два користувачі мають однаковий пароль, їх хеші будуть **різними**, бо у кожного — своя унікальна «сіль».

---

### 🧠 3. Спеціальні алгоритми

Для паролів **не використовують** звичайні алгоритми (MD5, SHA-256), бо вони надто швидкі — це дозволяє зловмисникам швидко підбирати паролі.

✅ Замість цього застосовують **повільні, захищені алгоритми**, наприклад:

* **PBKDF2** (використовується за замовченням у Django),
* **Argon2** (рекомендований),
* **bcrypt**,
* **scrypt**.

Ці алгоритми:

* додають сіль автоматично,
* виконують багато ітерацій хешування (уповільнюючи brute-force атаки).

---

### ⚙️ 4. Приклад у Django

Django робить усе це **автоматично**:

```python
from django.contrib.auth.models import User

user = User.objects.create_user(username='olga', password='mySecret123')
```

🔹 Django сам додає сіль і хешує пароль за допомогою PBKDF2 або Argon2.
🔹 У базі буде збережено не сам пароль, а рядок типу:

```
pbkdf2_sha256$600000$X9!2kf$0x12b9f2e...
```

---

### ✅ Підсумок

| Етап               | Що це                                           | Навіщо                                  |
| ------------------ | ----------------------------------------------- | --------------------------------------- |
| Hashing            | Перетворює пароль у хеш                         | Щоб не можна було прочитати пароль      |
| Salting            | Додає випадкові дані до пароля перед хешуванням | Щоб унеможливити порівняння або підбір  |
| Безпечний алгоритм | Використання PBKDF2, Argon2, bcrypt             | Щоб зробити підбір надзвичайно складним |

---
