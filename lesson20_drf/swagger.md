
**Swagger** — это инструмент, который автоматически **создаёт и визуализирует документацию для REST API**.
Его основная цель — сделать твой API **понятным, наглядным и удобным для тестирования** как для тебя, так и для других разработчиков или клиентов.

---

### 🔍 Что конкретно делает Swagger:

#### 1. **Генерирует интерактивную документацию**

Swagger автоматически создаёт веб-страницу, где можно:

* увидеть все доступные эндпоинты твоего API (URL, методы `GET`, `POST`, `PUT`, `DELETE`);
* прочитать описание каждого метода (если ты добавишь docstring или комментарии);
* увидеть, какие данные нужно передавать (поля, типы, обязательность);
* протестировать API прямо из браузера — отправить запрос и посмотреть ответ.

🖥️ Пример:
Открыв `/docs/`, ты увидишь интерфейс, где можно выбрать, например,
`POST /api/books/`, ввести данные книги и нажать “Execute” — результат вернётся тут же на экране.

---

#### 2. **Читает схеми (schemas) з DRF**

Swagger інтегрується з Django REST Framework і автоматично підтягує опис моделей, серіалізаторів, типів даних, токенів тощо.

---

#### 3. **Спрощує командну роботу**

Якщо над проєктом працює кілька розробників або фронтенд і бекенд окремо — Swagger стає “контрактом” між ними:
фронтенд бачить, які запити можна робити, які параметри передавати і які відповіді очікувати.

---

#### 4. **Підтримує OpenAPI стандарт**

Swagger створює документацію у форматі **OpenAPI Specification (OAS)** — це стандарт, який розуміють інші інструменти (наприклад, Postman або генератори клієнтів API).

---

### ⚙️ У Django REST Framework Swagger можна підключити так:

```python
# Встановлення бібліотеки
pip install drf-yasg
```

Потім у `urls.py`:

```python
from rest_framework import permissions
from drf_yasg.views import get_schema_view
from drf_yasg import openapi

schema_view = get_schema_view(
    openapi.Info(
        title="Library API",
        default_version='v1',
        description="Документація для API бібліотеки",
    ),
    public=True,
    permission_classes=(permissions.AllowAny,),
)

urlpatterns = [
    path('docs/', schema_view.with_ui('swagger', cache_timeout=0), name='swagger-docs'),
    path('redoc/', schema_view.with_ui('redoc', cache_timeout=0), name='redoc-docs'),
]
```

Після цього документація буде доступна за адресами:

* `/docs/` — інтерфейс **Swagger UI**
* `/redoc/` — альтернативний стиль документації **ReDoc**

---
# `SWAGGER_SETTINGS` в настройках

Эта часть настроек Swagger как раз **отвечает за авторизацию через токен (Bearer Token)** в твоей документации.

Разберём подробно, что делает каждая строка 👇

---

### 🔐 `SWAGGER_SETTINGS` объяснение:

```python
SWAGGER_SETTINGS = {
    'SECURITY_DEFINITIONS': {
        'Bearer': {
            'type': 'apiKey',
            'name': 'Authorization',
            'in': 'header'
        }
    },
    'USE_SESSION_AUTH': False,
    'JSON_EDITOR': True,
}
```

#### 🔸 `'SECURITY_DEFINITIONS'`

Это настройка, которая говорит Swagger, **какой тип авторизации поддерживает твой API**.
В данном случае ты указываешь **Bearer Token**, то есть токен, который передаётся в заголовке HTTP-запроса.

---

#### 🔹 `'Bearer'`

Это просто имя схемы (можешь назвать иначе, но "Bearer" — стандартное название для JWT/Token авторизации).

---

#### 🔹 `'type': 'apiKey'`

Говорит Swagger, что авторизация основана на **ключе API**, который клиент должен передать вручную (а не через сессию или cookie).
Для JWT это именно тот случай — токен передаётся вручную в заголовке.

---

#### 🔹 `'name': 'Authorization'`

Имя заголовка, через который передаётся токен.
Пример заголовка при запросе:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6...
```

---

#### 🔹 `'in': 'header'`

Указывает, что токен нужно передавать именно в **HTTP-заголовке**, а не в теле запроса или URL.

---

#### 🔹 `'USE_SESSION_AUTH': False`

Отключает стандартную **session-based** аутентификацию Django.
Это значит, что Swagger не будет пытаться использовать сессию браузера — только токен.

---

#### 🔹 `'JSON_EDITOR': True`

Включает удобный **JSON-редактор** в интерфейсе Swagger —
позволяет редактировать тело запроса прямо в JSON-формате при тестировании API.

---

### 💡 Результат:

После этих настроек в Swagger UI появится кнопка **“Authorize”**.
Когда ты нажмёшь её, откроется окно, где можно ввести токен, например:

```
Bearer your.jwt.token.here
```

Swagger добавит этот токен ко всем запросам автоматически ✅
и ты сможешь тестировать защищённые эндпоинты прямо из интерфейса.

---

